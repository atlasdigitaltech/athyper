import {readFileSync,writeFileSync} from 'node:fs';
import {createHash} from 'node:crypto';
import {parseEntityAiDescriptor} from '../../../server/packages/contracts/metadata/src/entity-ai.js';
const base=JSON.parse(readFileSync('docs/examples/atlas-f5/cirrus-bp-release-baseline.json','utf8'));
const proposed=JSON.parse(readFileSync('docs/examples/atlas-f2/business-partner.ai.json','utf8'));
const ai=parseEntityAiDescriptor(proposed,{entityCode:'business_partner',planeKey:'neon',fields:base.descriptor.fields,operationKeys:Object.keys(base.descriptor.operations)});
const candidate={...base.descriptor,ai};
const hash=(v:unknown)=>createHash('sha256').update(JSON.stringify(v)).digest('hex');
const receipt={schemaVersion:1,preparedAt:new Date().toISOString(),environment:'dev',tenantId:'44444444-4444-4444-8444-444444444444',entityCode:'business_partner',publicationKey:base.publicationKey,baseline:{releaseId:base.releaseId,releaseNo:base.releaseNo,compiledHash:base.compiledHash},author:'catl.admin',reviewer:'catl.owner',targetPlanes:['neon'],proposedDescriptor:candidate,proposedDescriptorJsonSha256:hash(candidate),changedTopLevelKeys:['ai'],validation:{aiSchemaAndFieldReferencesPassed:true,existingReadOperationPreserved:true,baselinePropertiesPreserved:Object.keys(base.descriptor).every(k=>hash(base.descriptor[k])===hash(candidate[k]))},publication:{status:'prepared_for_review',authenticatedAuthoringComplete:false,independentReviewComplete:false,signed:false,queued:false,activated:false},remainingChecks:['Refresh and verify both named Studio sessions.','Create a native change set from the retained tenant baseline, preserving the publication key and all existing runtime behavior.','Validate persistence and compilation round trip before submit and independent review.','Confirm the active tenant release has not changed before publication and verify the signed artifact and activation afterwards.'],note:'This is an AI descriptor delta and review packet, not a signed release or a native MetaEntityGraph 2.1 payload. Do not send this descriptor directly to the graph replacement endpoint.'};
writeFileSync('docs/examples/atlas-f5/cirrus-bp-ai-release-review.json',JSON.stringify(receipt,null,2)+'\n');console.log(JSON.stringify({prepared:true,validation:receipt.validation,changedKeys:receipt.changedTopLevelKeys,baselineRelease:base.releaseId}));
