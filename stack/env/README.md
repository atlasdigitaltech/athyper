# Env files

See [docs/infrastructure/env-reference.md](../../docs/infrastructure/env-reference.md) for the complete per-variable reference — descriptions, local/staging/production values, and secret flags for every variable across all env files.
