# Changesets

This folder is managed by [`@changesets/cli`](https://github.com/changesets/changesets).

Run `pnpm changeset` to create a new changeset describing your change.
Run `pnpm changeset version` to consume changesets and bump package versions.
Run `pnpm changeset publish` to publish updated packages.
