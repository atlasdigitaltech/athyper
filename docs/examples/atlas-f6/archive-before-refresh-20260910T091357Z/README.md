# Historical Atlas F6 receipts

The 14 distinct historical files remain here. The 41 byte-identical copies listed
in [duplicate-index.json](duplicate-index.json) were consolidated during cleanup.
The index records their original paths, SHA-256 values and matching current files.
Current files can evolve; use the checksum-verified private snapshot to recover
the exact historical bytes:

`~/.athyper/backups/docs-cleanup/20260914T041147Z/docs-before-cleanup.tar.gz`

Its sibling `manifest.json` records every original file. Extract selected members
to a temporary directory before restoring them. Prior qualification remains bound
to its original evidence; cleanup does not renew or change that qualification.
