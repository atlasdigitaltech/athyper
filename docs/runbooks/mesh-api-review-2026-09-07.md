# MESH Business Partner API review — 2026-09-07

> Local-development baseline update: the 16 scripts listed in [SQL DDL consolidation](sql-ddl-consolidation.md) have been folded into `server/db/ddl/` and removed from the migration manifests. Use the canonical DDL for a fresh local database. Migration commands and migration-path test results below describe the earlier implementation and are historical, not current setup instructions.

Reviewed all 15 requested routes in the local checkout: bank disclosures (4), network workspace/relationships/capabilities/registration exchanges (7), and profile publications (4). Traced route parsing, verified-context authorization, service commands, repository queries, lifecycle SQL, participant RLS, and audit/outbox construction. Fixed the confirmed issues below.

| Priority | Finding and impact                                                                                                                                                                                                               | Fix                                                                                                                                                                                                                                                           |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1       | Bank decision and revocation retries returned payloads before checking the caller's current permission.                                                                                                                          | Require the operation permission before returning replay data.                                                                                                                                                                                                |
| P1       | Bank revocation keys could return another disclosure; decision keys did not distinguish approval, rejection, actor, reason, or secure reference. Request keys ignored owner, purpose, and expiry.                                | Compare the original command coordinates and event evidence; return 409 on conflicting reuse. Replay responses now use the current lifecycle version, consistent with the returned current disclosure state.                                                  |
| P1       | Relationship creation passed the selected account as command input but never set the database session account required by `command_request_network_relationship`.                                                                | Set transaction-local `app.current_network_account_id` from the verified service coordinate before executing the command.                                                                                                                                     |
| P1       | Every policy-approved self-registration supplied `policyEvaluation` to a SQL command that rejected that field. Including changing evaluation IDs in fingerprints would also break retries.                                       | Accept server-generated evaluation metadata, store it in command evidence, remove it from recipient-visible intent and retry fingerprint, and reject caller-supplied evaluation metadata at the service boundary. Canonical SQL and a migration are included. |
| P2       | Bank revocation outbox messages included the retained bank payload instead of the withdrawal reason.                                                                                                                             | Emit a withdrawal envelope containing the reason without the retained secure reference.                                                                                                                                                                       |
| P2       | A pending bank disclosure could be approved after expiry. Invalid or past request expiry could reach a database cast/check failure.                                                                                              | Validate request expiry and reject approval after expiry.                                                                                                                                                                                                     |
| P2       | Concurrent bank requests/decisions and profile publications/withdrawals could both miss replay evidence, then collide on unique keys or lifecycle/version allocation.                                                            | Acquire a transaction advisory lock before replay lookup. Each tenant's bank commands and profile commands have separate lock namespaces. Withdrawal re-reads state after acquiring the lock.                                                                 |
| P2       | A valid 200-character publication key generated a 208-character event key, violating the database limit.                                                                                                                         | Hash the generated publication-event key to a fixed length.                                                                                                                                                                                                   |
| P2       | Withdrawal replay accepted a different reason or actor for the same publication.                                                                                                                                                 | Compare persisted withdrawal reason and actor before replay.                                                                                                                                                                                                  |
| P2       | Network SQLSTATE errors for denied participation, missing records, stale versions, invalid transitions, malformed inputs, and conflicting keys became HTTP 500.                                                                  | Translate known database errors at the network service transaction boundary into sanitized 400/403/404/409 domain errors. Unexpected errors remain unexpected errors.                                                                                         |
| P2       | Bank/network routes coerced arrays, objects, booleans, and numeric strings into strings or versions; false optional values were silently omitted. Date strings were not validated, and repeated publication limits were coerced. | Strict JSON scalar validation, calendar-date and timezone-qualified timestamp parsing, presence-based optional fields, and scalar query-limit parsing. Canonicalize UUID case before comparing replay coordinates.                                            |
| P2       | Profile publication and withdrawal ignored the idempotency header supported by the adjacent APIs. Workspace reads omitted the service's MESH-plane check.                                                                        | Add header fallback and reject workspace calls from another plane.                                                                                                                                                                                            |

Validation:

- `pnpm --filter @athyper/server-plane-mesh test`: **89 tests passed**, including real HTTP dispatch/authentication/replay checks for every requested endpoint, malformed-input regressions, bank authorization and replay checks, expiry/revocation tests, SQLSTATE translation, and compiled repository SQL checks.
- `pnpm --filter @athyper/server-plane-mesh typecheck`: passed.
- `node server/db/scripts/tests/integration/mesh-api-review.mjs`: passed against disposable PostgreSQL 16.13. Executes both the canonical registration function and migration; verifies original policy evidence retention, recipient intent exclusion, replay with fresh evaluation metadata, changed-command conflicts, and expired-new-request rejection.
- Scoped `git diff --check`: passed.

Deployment and verification boundaries:

- Apply `server/db/migrations/20260907_mesh_api_review.sql` through the normal MESH migration process together with the service changes. It is appended to the existing MESH manifest. No application database or development API was modified.
- The PostgreSQL regression uses minimal dependency tables and an evidence-recorder fixture. It does not exercise the full deployed schema, IAM/RLS environment, external policy provider, or delivery workers.
- Advisory locking is deliberately tenant-wide within each publication/disclosure family so command keys, aggregate versions, and supersession share a consistent lock order. This reduces write concurrency within one tenant. SQL ordering was checked; production-load concurrency was not benchmarked.
- Existing unrelated workspace changes were preserved. The touched route/service files were formatted to make the reviewed logic readable.
