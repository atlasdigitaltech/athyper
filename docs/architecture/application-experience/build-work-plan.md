# Shared Application Experience — Local Build Plan

Build and clean up the current local project. One developer, one local environment, frontend and backend developed together. No production rollout, owner assignment, release cohorts, feature flags, document versioning or replica work is required by this plan.

Use [System Design](system-design.md) for component behavior. Existing repository authorization and data-handling rules still apply; simplifying delivery does not remove business workflow checks.

## Working rules

- Reuse one Main/PageWorkspace and shared page-kind runtimes. Do not copy Business Partner layouts for new entities.
- Keep app routes thin; put reusable behavior in existing platform packages and specialized behavior in providers.
- One main landmark, one page h1, one primary page scroll surface.
- No entity-specific layout branches in generic components.
- Client-side visibility is not authorization. Keep server checks and context isolation.
- Keep visible keyboard focus and labels on icon buttons; mobile modal panels must open/close and return focus correctly.
- Preserve unrelated local edits. Make small changes, check the affected behavior, then continue.
- Do not delete old code merely because a text search finds no callers. Check routes, exports, registries, assets and package/build references first.

## Build tasks — roughly in this order

### 1. Inspect what exists

- [x] Identify the active Business Partner collection, detail/360, intake and Studio composition implementations.
- [x] Identify existing shared headers, section navigation, forms, loading/error components and duplicate CSS.
- [x] Record only the files being changed and their replacement; use a short working checklist, not a repository-wide governance inventory.
- [x] Check current local modifications before editing. Capture enough baseline behavior to recognize a regression.

**Inspection baseline (2026-09-17):** working tree was clean before this task; no inherited local edits needed preservation. No application code has been changed during inspection. The map below is the intended edit scope, not a deletion list.

| Active entry / implementation | Reuse or planned treatment |
| --- | --- |
| `apps/neon/app/(shell)/mdg/business-partner/layout.tsx` → `apps/neon/lib/entity-application-layout.tsx` → `packages/planes/neon/list-view/src/index.tsx` | Retain entityCode-driven application composition; adapt shared workspace chrome |
| Business Partner `page.tsx` / `manage/page.tsx` → `NeonEntityApplicationSection` | Overview and collection are already shared sections; do not replace with a new entity-local list |
| `partners/page.tsx` and `business-partners/[recordId]/page.tsx` | Existing redirect aliases; preserve URLs/query behavior, not duplicate pages |
| `[recordId]/page.tsx` → `BusinessPartnerRecord` in `packages/planes/neon/business-partner/src/index.tsx` | Validate ID; preserve existing `neon.business_partner.view_360` selection and aggregate fallback until both are understood |
| `packages/planes/neon/business-partner/src/360/business-partner-360.tsx` → shared `EntityRecord360Panel` | Retain authorized providers, URL state, tabs and section behavior; adapt geometry rather than rewrite domain sections |
| `new/page.tsx` → `AuthorizedNewBusinessPartnerRequest` → request entry / `NewBusinessPartnerRequest` | Preserve permission gating, request flow, draft values and governed submission |
| `packages/platform/entity/runtime/form-detail/src/{intake,intake-state,form-layout,section-navigation,record-360-panel,section-workspace}` | Reuse intake state and scroll navigation; consolidate layout wrappers around them |
| `apps/studio/app/(shell)/mdg/business-partner/{layout.tsx,module-navigation.tsx,workbench-context.tsx,model/page.tsx}` | Preserve draft context and module navigation; model route already wraps `BusinessPartnerInspection` in PageFrame |
| `packages/planes/studio/business-partner/src/{workbench,composition-workspace,composition-navigation,composition-editor,composition-review}.tsx` | Keep specialized editor behavior; extract only shared workspace orchestration |
| `packages/platform/shell/shell/src/{client,page-foundation,entity-page-layout,management-workspace,record-footer}.tsx` | Existing shared shell/header/record-ownership/footer foundation; first consolidation target |
| `apps/{neon,mesh,studio}/app` loading/error/provider/layout adapters and `packages/platform/shell/app-foundation/src/boundaries.tsx` | Inspect reuse before introducing another fallback implementation; existing AppErrorBoundary is an explicit error-presentation component, not by its name proof of render catching |

**CSS consolidation targets:** `packages/platform/foundation/ui/src/styles.css` currently owns both record-360 and section-workspace geometry. Record-360 derives sticky offsets from shell/tab variables, whereas section-workspace uses `top:8rem`. Shared navigation typography also lives here. Shell geometry is in `packages/platform/shell/shell/src/styles.css`; domain-specific styling remains in `packages/planes/neon/business-partner/src/styles.css` and `apps/studio/app/(shell)/mdg/business-partner/workbench.css`. These are overlapping responsibilities to consolidate, not evidence that whole files are redundant. Preserve record mobile selectors, overview reflow, Studio tables/editor overflow and shell document scrolling.

**Executed baseline checks:**

- `pnpm --filter @athyper/product-neon-business-partner test -- src/intake-runtime.test.tsx src/360/record-opening.test.tsx` — passed, **46 files / 225 tests**. The script invocation ran the full package suite rather than only the requested files.
- `pnpm --filter @athyper/product-studio-business-partner test -- src/composition-navigation.test.tsx src/composition-workspace.test.tsx` — passed, **23 files / 78 tests**; likewise ran the full package suite.
- `pnpm exec playwright test --config tooling/config/playwright.foundation.config.ts tests/foundation-browser/business-partner-section-layout.spec.ts tests/foundation-browser/business-partner-360-panel.spec.ts` — **blocked before browser execution**: esbuild cannot import `react-day-picker/src/style.css` from `packages/platform/foundation/ui/src/date-picker-calendar.tsx` without an output path. No browser or visual pass is claimed.

**Follow-up completed:** both browser fixtures now give esbuild a virtual output path, select emitted JavaScript explicitly, and inject emitted CSS alongside existing fixture styles. Production date-picker styles remain unchanged. The 360 fixture now uses the actual presentation configuration, current section response fields, primary-record IDs, seven tab labels and accessible section-selector name. Its scroll assertion waits for lazy content to settle and verifies clearance below the sticky tabs. The three selected Playwright tests pass. These are isolated fixtures, not a live authenticated application walkthrough.

**Task 2 started:** `BrowserApplicationProviders` in app-foundation now owns the previously duplicated browser query/API-client initialization. Neon, Mesh and Studio retain thin router adapters and their existing server bootstrap; Neon retains ShellRouteProvider. Provider nesting, CSRF handling, lifecycle callbacks and authentication destinations are preserved. App-foundation and all three apps passed typecheck; all nine query/provider lifecycle contract tests passed. Other task 2 items are still open.

**Task 2 continued:** shared `ApplicationLoading` and provider-independent `ApplicationFatalError` adapters now wrap the existing app-foundation boundaries and source plane identity from the brand catalog. All three apps use them for root loading, shell loading and framework fatal errors. Existing collapse-cookie handling and reload recovery remain intact. Startup-error consolidation remains open.

The shared shell now delegates its skip link and footer to `shell-chrome.tsx`, preserving `RecordFooterSource`. `use-shell-surfaces.ts` coordinates navigation, header panels and Recent/Favorites through one reducer: opening a surface replaces the previous one, and closing an inactive surface cannot dismiss another kind of surface. Opening Atlas dismisses these transient surfaces; Atlas pin/full-screen lifecycle and context-picker coordination remain to be extracted. Business routes, intake authorization, record/collection header ownership and Studio editor state were not changed.

**Validation for this increment:** app-foundation, shell and all three apps passed typecheck; 32 focused fallback, error-boundary, surface-state and query/provider tests passed. The three Business Partner browser tests still pass. The shared-shell fixture received the same CSS-preserving bundling correction. Its 14 tests produced 9 passes and 5 failures; all five also reproduced using the original `HEAD` shell client with the same corrected fixture. The temporary comparison fixture was removed. This is not a clean shell browser pass.

**Shell baseline cleanup completed:** all five browser failures are resolved. The fixture now includes the production UI drawer stylesheet and checks the current `/home` destination, Home-first navigation focus, static single-context display and compact phone branding. The activity-panel test uses a non-home route so its breadcrumb checks are meaningful. Production styling was preserved. The real Escape bug was in header actions: an inactive header listener cleared another surface and could restore focus to an old opener. Only an active search/agent panel now handles that listener. A regression test switches from search to navigation and Recent, then verifies Escape restores focus to the navigation trigger.

**Shell extraction continued:** `shell-chrome.tsx` owns GlobalAppBar and GlobalSidebar layout slots alongside the footer/skip link. Header actions and navigation/Recent/Favorites controls moved into `shell-header-actions.tsx` and `shell-navigation.tsx`; `shell-i18n.ts` holds their existing shared localization fallback. `shell-overlay-host.tsx` owns Recent/Favorites and Atlas rendering without introducing a positioning wrapper. Header-anchored search/activity panels, context selection, and Atlas state transitions still retain their existing owners; full overlay coordination is not claimed complete.

**Latest checks:** the 17 existing selected browser tests pass, plus the new Escape regression test (18 total). Shell and Neon/Mesh/Studio typechecks pass. The 32 focused fallback/surface/error/provider tests pass; 14 of 15 shared-shell navigation contracts pass after updating source checks for the extracted modules and formatting. The remaining Atlas-home source-text assertion expects `timeZone }).format(date)` while the unchanged implementation formats this across lines with a trailing comma; that broader test was already stale and is outside this shell increment. Neon Business Partner: 46 files / 225 tests passed. Studio Business Partner: 23 files / 78 tests passed. These are isolated tests, not a live authenticated application walkthrough.

**Startup/overlay increment completed:** all six root and authenticated-shell error routes now delegate to `ApplicationError` in app-foundation. The installed framework's `retry` refreshes server rendering; plain `reset` only clears error state, so recovery remains `retry` or document reload. Root errors retain one main landmark; content errors retain the surrounding shell. Provider-independent rendering and diagnostic redaction are covered.

`use-shell-surfaces.ts` now uses `shell-overlay-state.ts` to coordinate header actions, overflow, navigation, context-picker identity and Atlas presentation. A new transient surface dismisses unpinned Atlas; a pinned desktop dock can coexist. Expanding Atlas takes the transient channel, minimizing retains the prior pin preference, and compact presentation does not overwrite that preference. The same Atlas workspace and prompt draft survive expand/minimize. Tenant changes clear active surfaces; route changes dismiss transient surfaces while preserving a desktop dock. Context pickers use the shared controller when mounted inside the shell and retain their standalone behavior otherwise. Existing context discovery, switch authorization and business workflow callbacks remain in place.

`ShellSurfaceBoundary` adds local, redacted render recovery for Search, Activity, Recent/Favorites, context content and Atlas. Recovery does not remount page input. Atlas close returns focus to its opener; replacement by another surface no longer restores focus to an obsolete opener. Modal focus containment/background inertness still needs its separate final audit below.

Compact app-bar priority now keeps Search and Inbox directly available through 760px, moving Notifications and Atlas into labeled overflow. Below 361px Inbox is also in overflow. All four actions remain reachable there. `activity-counts.ts` distinguishes unavailable/loading/failed/partial/invalid totals from successful zero; header badges, activity tabs and pending filter counts no longer manufacture zero totals.

**Validation:** 22 selected browser tests passed, including Business Partner section/history baselines, context/header replacement, compact overflow, Atlas draft preservation and unknown/zero counts. All 37 focused fallback, retry, provider, overlay-state and local-recovery tests passed. Neon Business Partner (225 tests) and Studio Business Partner (78 tests) also passed again. Shell/app-foundation and all three app typechecks passed. Navigation/activity client contracts passed (17 tests) with the previously deferred Atlas-home source-text test excluded explicitly. An additional legacy JSDOM activity-interaction run was blocked by `React is not defined` in the existing `AtlasBrandIcon` under the tsx runner; the equivalent browser interactions pass. That runner issue and Atlas-home source-contract cleanup remain separate; no production icon/style workaround was added.

**Modal/preferences/footer audit completed:** `platform-ui/src/modal-isolation.ts` now provides a shared modal stack for dialogs/drawers, mobile navigation, mobile Quick Access, transient/expanded Atlas and local surface-error recovery. It contains keyboard/programmatic focus, excludes hidden/disabled controls, handles empty dialogs, makes background branches inert and locks document scrolling. Closing a nested modal restores its parent; closing the last modal restores prior inert flags and overflow. Portaled menus register with their owning modal and focus only after positioning. Closed mobile navigation is inert; desktop Quick Access and pinned Atlas remain nonmodal. Existing navigation/context labels remain attached to their controls.

Appearance and density continue to use the one shared app-foundation profile provider across all three apps; theme contracts pass. Shell preference reads/writes now tolerate denied storage, use the existing collapse cookie as fallback and avoid persisting automatic viewport adaptations. Recent/Favorites retain one implementation. Recent records authorized pathname entries without requiring the panel to open; query/tab changes do not create another visit. Cached items are filtered against current route access before display, and durable bookmarks without an authorized entity route are omitted. The local cache is now v2 and partitioned by plane, tenant and principal. Legacy v1 data is retained but not automatically imported because its plane is ambiguous; the new cache starts fresh. Record-level success/identity reporting belongs to the upcoming PageWorkspace runtime.

Footer registration now tags route scope and owner identity. Scope changes hide prior provenance without remounting children, revoked registrations clear it, and an old owner's cleanup cannot erase a newer registration. Mesh and Studio now use the same ShellRouteProvider as Neon. Existing app-foundation session/access boundary keys continue to reset scoped children on authenticated-context invalidation.

**Latest validation:** 29 browser checks passed (28 selected shell/footer/Business Partner checks plus the explicit-cookie fallback test). All 54 focused primitive/theme/fallback/provider/overlay/footer tests, 17 navigation/activity contracts and 303 Business Partner/Studio tests passed. UI, shell, app-foundation and all three apps passed typecheck. The deferred Atlas-home source-text test remains excluded; its separate cleanup and the legacy JSX runner issue were not folded into this work.

**PageWorkspace initial increment completed:** `platform-shell/src/page-workspace.tsx` composes the existing PageFrame/PageHeader and exposes header, navigation, status, toolbar, body, actions and action-outcome slots. It accepts an existing record header without creating another h1. PageLayout supplies content, sections-content and sections-content-overview variants; columns collapse using existing shell breakpoint conventions and spacing tokens, with no new scroll container. Slot content retains responsibility for semantic navigation and status/error announcements. This is composition, not a resource-state controller.

The generic EntityDetailRuntime now uses PageWorkspace for both loading and ready content. EntityPageLayout/useRecordPage registration is unchanged, including its existing post-mount effect timing. Specialized Business Partner detail/intake and Studio composition remain on their existing runtimes for incremental adoption; this does not claim their workspace migration is complete. No authorization, focus/history handlers or draft state were moved.

**Increment validation:** two new PageWorkspace browser tests pass for header handoff, retained input/focus/scroll during status/action updates, responsive column layouts and absence of nested scrolling. Three existing Business Partner browser tests pass for section layout, explicit navigation, deep links and history. Neon/Studio Business Partner suites pass (303 tests); shell, form-detail and all three apps pass typecheck. Header/intake foundation checks also pass. The initial browser assertion was adjusted to wait for the existing effect-based header registration before checking the single heading; production ownership timing was not changed.

**Next action:** incrementally adopt workspace slots in the existing collection/detail/intake/Studio compositions, preserving current header owners and navigation controllers. Centralize sticky offsets, then add resource/render recovery and successful-page identity/footer reporting. Keep authorized intake, one primary scroll root, explicit-focus/history behavior and Studio draft state intact.

### 2. Consolidate application startup and shell

- [x] Reuse existing app-foundation and plane shell adapters; avoid duplicate bootstrap requests.
- [x] Share root/shell loading and provider-independent fatal-fallback adapters.
- [x] Consolidate startup-error adapters using existing bootstrap behavior.
- [x] Extract shared app-bar/sidebar slots, navigation/header controls, footer and Recent/Atlas overlay rendering without changing business routes.
- [x] Finish header/context/Atlas overlay coordination, including active-surface recovery.
- [x] Fix active-surface Escape/close ownership, mobile action overflow and unknown badge states.
- [x] Audit modal focus containment/background inertness and remaining context labels.
- [x] Keep one shared preferences and Recent/Favorites implementation.
- [x] Keep footer record information page-owned and clear it when the page/context changes.

### 3. Build PageWorkspace

- [x] Compose existing PageFrame/PageHeader and preserve EntityPageLayout collection-versus-record ownership; initial integration in generic EntityDetailRuntime.
- [x] Add content, sections-content and sections-content-overview layouts with responsive columns; specialized workspace adoption remains incremental.
- [ ] Use the existing primary page scroll root and shared spacing tokens; centralize sticky offsets instead of adding another scroll container.
- [x] Add header/navigation/status/body/toolbar/action and action-outcome slots.
- [x] Adopt slots in remaining specialized runtimes — investigated by grepping for `PageSurface` beyond the files already touched, rather than assuming they were clean. Found the same duplicate `<main>`/`<h1>` bug in 9 more files across two packages the earlier pass hadn't reached: 7 more Business Partner files (`mesh-proposal-experience.tsx`, `customer-controls.tsx`, `request-entry.tsx` ×2, `banking-workspace.tsx`, `role-extension-experience.tsx` ×2, `supplier-controls.tsx`, `customer-request.tsx`) and a whole separate plane package, Workforce (`supplier-requisitions.tsx`, `index.tsx` ×3). Fixed all of them. For Business Partner: extracted `BusinessPartnerPageFrame` out of `index.tsx` into its own `page-frame.tsx` first — `index.tsx` already imports from several of these files (`request-entry.tsx`, `customer-controls.tsx`, etc.), so importing the helper back from `index.tsx` would have created a circular dependency; extracting it to a neutral file let every file, including `index.tsx` itself, import from one place safely. `banking-workspace.tsx` had a genuine second finding, not just the mechanical swap: its `embedded` prop (used when rendered inside the 360 panel's Roles tab) never suppressed the header, so the embedded case was unconditionally nesting a duplicate landmark inside an already-page-owned tree; added `contentOnly={embedded}` to fix it, matching the intake-wizard pattern already established elsewhere. For Workforce: a genuinely separate plane package, so reusing Business Partner's helper would have violated the plane-isolation invariant ("plane implementations do not import another plane's internals") — added `@athyper/platform-shell` as an actual declared dependency (it wasn't one before), ran `pnpm install` to link it (minimal, 5-line lockfile diff, checked), and built a small local `WorkforcePageFrame` mirroring the same pattern without the `toolbar`/`contentOnly` options nothing there needs. Verified: typecheck clean for both packages plus the Neon app itself (which consumes both), all 225 Business Partner tests and 4 Workforce tests pass, and the shared-shell/360-panel/section-layout browser specs (28 tests total) still pass.
- [x] Publish successful page identity/readiness for Recent — found and closed a real, separate gap from footer provenance, not the same blocker. `useRememberQuickAccessVisit` (`packages/platform/shell/shell/src/quick-access.tsx`), which already runs centrally for every route via `client.tsx`, derives its Recent-entry label purely from route config and a humanized URL segment — and explicitly excludes UUIDs from that humanization, meaning every record-detail page whose URL segment is an opaque ID (Business Partner's `[recordId]` routes included) fell back to a generic workspace label instead of the record's real title. Meanwhile `useRecordBreadcrumb` — already called throughout Business Partner and generic elsewhere — registers exactly that resolved title on the same `ShellRouteContext` that `client.tsx` already reads (`routeState = useShellRoute()`) one line above the `useRememberQuickAccessVisit` call it was never passed to. Wired it through: added an optional `resolvedRecord` parameter to `useRememberQuickAccessVisit`, used only when its registered `pathname` matches the current path, preferred over the humanized-segment fallback. No page needs to register anything new — this reuses the existing, already-generic breadcrumb registration, so every current and future page that calls `useRecordBreadcrumb` benefits, not just Business Partner. Verified: typecheck clean for `@athyper/platform-shell`, and the two most directly relevant browser tests ("quick access tracks recent work and promotes it to persistent favourites," "Recent records visits before opening and Favorites remain scoped and access-filtered") both pass, along with the full 28-test shared-shell suite.
- [ ] Structured footer provenance — still genuinely blocked, unchanged from the earlier finding: `RequestView`/`PartnerAggregate` carry no `provenance`/source-observed field for `useRecordFooterSources` to register, and using `updatedAt` as a stand-in would conflate "record updated" with "source observed" (invariant #8). Needs a real backend field; not a frontend gap.
- [x] Reuse section navigation for explicit scroll or switch mode; preserve deep links and Back/Forward — investigated, found substantially already done. `EntitySectionNavigation` + `useEntitySectionScroll` (`packages/platform/entity/runtime/form-detail/src/section-navigation.tsx`) already implement scroll-mode section navigation matching §7.3 almost exactly, down to the exact invariant stated in its own doc comment: "Explicit navigation scrolls/focuses; passive scroll observation never moves focus." Already generic, already reused by `EntityRecord360Panel`. Note: this is a distinct concept from the `PageNavigation` component built earlier this session — `PageNavigation` (Tabs/RouteLinks/WorkflowSteps) is the page-level navigation kind from Appendix A, while `EntitySectionNavigation`'s scroll/switch modes are `PageLayout`'s separate `SectionNavigation` concept. Not yet wired through `PageLayout`'s formal `sectionNavigation` slot for any Business Partner surface, but that's because none of the surfaces touched this session use the `sections-content`/`sections-content-overview` layout variant yet (all flat `content`, or `PageNavigation` tabs) — no consumer needs that wiring yet, so it wasn't forced.
- [x] Add shared loading/error/empty/denied states and independent section/panel recovery — `PageResourceBoundary` already covers the page-level loading/error/empty/ready piece. Investigated section/panel-level recovery separately: `EntityRecord360Panel`'s `ProgressiveSection` (used by every 360 panel, including Business Partner's) had zero render-error isolation — confirmed by inspection, no `componentDidCatch`/`getDerivedStateFromError` anywhere in the file. A render exception in any one section (e.g. `CommonSection`, `RolesWorkspace`, `NetworkSection`) would propagate up and take down the whole panel, not just that section. Added `SectionBoundary`, a class-based error boundary matching the one existing precedent in the codebase (`ShellSurfaceBoundary` in `packages/platform/shell/shell/src/shell-surface-boundary.tsx` — same shape: local `failed` state, `Try again` resets it), wrapping each section's content inside `ProgressiveSection`. Added matching CSS (`.a-record-360__section-error`) reusing existing tokens (`--a-border`, `--a-radius-md`, `--a-space-*`), consistent with the file's existing `.a-record-360__placeholder` rule right above it. This is genuinely shared: it benefits every current and future consumer of `EntityRecord360Panel`, not just Business Partner. Verified: typecheck clean for `@athyper/platform-entity-form-detail`, 225/225 Business Partner tests pass, and both relevant browser specs (`business-partner-360-panel.spec.ts`, `business-partner-section-layout.spec.ts`) pass.
- [x] Preserve input during refresh or failed save; disable dependent mutations when required content fails — audited every surface touched this session individually rather than assuming; found already correctly implemented everywhere, not something needing new code. **Preserve input on failed save:** checked every `catch` block across `NewBusinessPartnerRequest` (intake `answers`), `BusinessPartnerRequestEdit` (uncontrolled `defaultValue` inputs, whose DOM values survive since the tree doesn't unmount on error), `BusinessPartnerRequestDetail`, Studio's `workbench-editor.tsx`/`composition-editor.tsx` `save()` (both explicitly message "Your local changes/edits are retained" and never reset `graph`, plus set a `blocked` flag preventing automatic replay — a more sophisticated version of this than anywhere else in the codebase), and Mesh's `NetworkExchangeWorkspace` `command()` (controlled inputs, untouched on failure). None clear form state on failure. **Disable dependent mutations when required content fails:** `BusinessPartnerRequestDetail`'s command bar and `BusinessPartnerRequestEdit`'s form are both gated behind `if (!view) return <PageResourceBoundary .../>`, so they don't exist at all until required data loads — nothing to separately disable. Mesh's relationship/capability/registration action buttons only render via `workspace?.relationships.map(...)`, so they're absent, not just disabled, when `workspace` fails to load. Studio's case is the most explicit: `BusinessPartnerInspection` reads `{ inspection, loading, error }` from the same shared `Context` that `BusinessPartnerWorkbench` populates and guards with `if (!inspection || loading || error) return <section>...no editor rendered, with the message "Configuration cannot be displayed until the read succeeds."...</section>` — the composition editor and its Save action are structurally absent whenever required content hasn't loaded successfully. No code changes were needed; this closes out the last unaudited Task 3 item.

### 4. Define the shared entity runtime contract

- [ ] Use pageKind as the sole selector: collection, detail, create, edit, intake, review, configuration. No template field.
- [ ] Define common fields plus kind-specific requirements; keep layout/mode explicit in published definitions.
- [ ] Keep provider-specific settings under validated options and bind provider identifiers to local registered implementations.
- [ ] Put the canonical browser-safe definition/parser in the existing contract family; reuse it from frontend and backend. Do not manually mirror wire shapes.
- [ ] Reuse existing schema versions where compatible; use a distinct new contract when needed rather than silently changing an existing parser's meaning. Update local consumers together.
- [ ] Validate identifiers, references, options, page-kind/slot combinations and operations. Reject missing required providers; omit optional ones only when the task remains valid.
- [ ] Validate on the backend before publication and defensively at render time. Check the current plane's registered providers and applicable tenant capabilities; retain request authorization.
- [ ] Cover a few meaningful invalid definitions with tests, including an unknown required provider and a broken reference.

Generic layout/state work in task 3 can proceed without finalizing descriptor details. Finalize task 4's actual contract before wiring the resolver, provider bindings and publication parser to it. No formal decision register is needed.

### 5. Migrate Business Partner directly

- [x] Fix duplicate `<main>`/`<h1>` in Business Partner surfaces: replaced non-`contentOnly` `PageSurface` usages in `packages/planes/neon/business-partner/src/index.tsx` (scope-configuration, home, request list, request-detail loading/error, aggregate detail, request-edit, unauthorized-intake) with a local `BusinessPartnerPageFrame` helper that composes the shared `PageWorkspace`. Header-owner call sites (`RequestDetailSurface`'s always-`contentOnly` pattern, and the intake-wizard-owned branch of `NewBusinessPartnerRequest`) were left on the existing pattern, only swapped to the same helper so the intake case also stops rendering a duplicate landmark when not wrapped by an intake shell. No business logic, header ownership, or props changed — structural only. Verified: package typecheck clean, all 225 existing tests in the package pass. `pnpm test:shared-shell-phase8`'s `tsx --test` step has one pre-existing unrelated failure (brittle source-string check on `PlatformHome`, confirmed present on unmodified `HEAD` too); not introduced by this change and not fixed here.
- [x] Adopt PageWorkspace's `toolbar` slot: `BusinessPartnerRequestList`'s and `BusinessPartnerAggregateDetail`'s organization/status filter cards now render through `BusinessPartnerPageFrame`'s new `toolbar` prop (mapped to `PageWorkspace`'s `toolbar` slot) instead of sitting as plain body children — matches the shared convention that the content toolbar owns filters. Verified: typecheck clean, 225/225 tests pass.
- [x] Adopt the `actions` (bottom command-bar) slot for `BusinessPartnerRequestDetail`'s Validate/Submit/Approve/Reject/Apply command bar. `RequestDetailSurface` now branches explicitly on its existing `shared` flag: when an ancestor task header owns the header row (`shared === true`), it stays exactly as before — a bare `contentOnly` section, header untouched, command bar still an ordinary trailing child, because there's no ancestor mechanism to register a bottom action bar (only `useRegisterEntityTaskHeader` exists, and it only carries header fields). When this component is the outermost owner (`shared === false`), it now renders a real `PageWorkspace` with `header` built from the exact same memoized header object as before (`level: "collection"`, same title/actions/metadata/supportingRow) and the command bar passed as `actions`, giving it the dedicated, space-reserving action-bar slot instead of an ordinary trailing `Card`. The command bar itself was extracted verbatim from the caller (`BusinessPartnerRequestDetail`) into a `commandBar` local and passed down as a new prop — no button, permission check, or handler logic changed. Removed the now-unused local `<PageHeader>` render and its import. Verified: typecheck clean, 225/225 tests pass.
- [x] Adopt the `status` slot for `BusinessPartnerRequestDetail`'s reload/mutation-error notice (the "Refresh failed" case). Extracted the existing `ErrorNotice` (unchanged: same `detail`/`mfa` logic) out of the body into a `statusNotice` local, passed to `RequestDetailSurface` as a new `status` prop. `!shared` branch routes it to `PageWorkspace`'s `status` slot (matches the design's PageStatusRegion intent). `shared` branch cannot split its opaque `children` blob, so the notice now renders immediately before `children` instead of between the case summary and the tabs — a one-position, purely cosmetic reorder, called out explicitly rather than silently changed. Verified: typecheck clean, 225/225 tests pass.
- [x] Build `PageNavigation` (tabs kind only — no route-links/workflow-steps yet, nothing needs them) and `useDeepLinkedTabState` in `packages/platform/shell/shell/src/page-navigation.tsx`, exported from the package barrel. `PageNavigation` composes the existing `Tabs`/`TabsList`/`TabsTrigger`/`TabsContent` primitives from `platform-ui` (accessible keyboard selection and `TabsContent`'s `mount: "lazy"` manual-activation case already came from those primitives; not reimplemented) and adds an `aside` slot for content that must stay visible across all tabs. `useDeepLinkedTabState` generalizes `BusinessPartnerRequestDetail`'s exact prior hash+session-storage sync logic (`normalize` and `storageKey` are caller-supplied, so the BP-specific alias mapping in `requestTab` stays in the plane package, not the shared hook); it still always uses `history.replaceState`, matching current behavior — no push-history option added speculatively. Wired `BusinessPartnerRequestDetail`'s Tabs through both: `activeTab`/`selectTab`/the hashchange `useEffect` were replaced by one `useDeepLinkedTabState` call, and the raw `<Tabs>` tree became `<PageNavigation kind="tabs" .../>`, with `RequestLifecycle`/`SupplierProcessCorrection` (previously interstitial children between the tab list and panels) moved into the new `aside` slot to preserve their always-visible-regardless-of-tab position. `PageNavigation` renders inside `RequestDetailSurface`'s body (`children`), not `PageWorkspace`'s separate `navigation` prop — that prop is a content-free strip, and this component intentionally bundles the tab strip with its panels (matching Appendix A's "Tabs → Associated tab panels" grouping), so putting the whole bundle in `navigation` would visually invert the page order. Verified: typecheck clean for both `@athyper/platform-shell` and the Business Partner package, 225/225 existing tests pass. No pre-existing test covered the hash/storage sync logic directly, so nothing was silently left untested by the extraction, but a small hook-level test is a worthwhile follow-up now that it's shared infrastructure — not added here.
- [ ] Centralize sticky offsets — investigated and deferred: none of these BP surfaces use sticky positioning or section navigation yet, so there is no call site to attach shared offset tokens to. Premature until scroll/switch-mode section navigation is actually adopted.
- [ ] Add successful-page identity/footer reporting for `BusinessPartnerRequestDetail`/`BusinessPartnerAggregateDetail` — investigated and deferred: the mechanism (`useRecordFooterSources`) already exists and is already used elsewhere in this package (`360/components/common-section.tsx`, registering `section.provenance`), but `RequestView`/`PartnerAggregate` (checked `client.ts`) carry no `provenance`/source-observed field to register. Using `request.updatedAt` as a stand-in would conflate "record updated" with "source observed," which the design explicitly treats as distinct timestamp meanings (invariant #8) — not doing that. Needs a real provenance field from the backend before this is adoptable here; not a frontend gap.
- [x] Add resource/render recovery: built `PageResourceBoundary` (`loading | error | empty | ready`, in `packages/platform/shell/shell/src/page-resource-boundary.tsx`) — four states, matching exactly what `BusinessPartnerRequestDetail` and `BusinessPartnerRequestEdit` already branched on by hand, not the full forbidden/not-found taxonomy nothing here differentiates yet. Loading is now wrapped `aria-busy="true"`, which neither surface did before (a real, pre-existing accessibility gap, confirmed by inspection — not hypothetical). Wired both surfaces' early-return branches through it. Also added an optional `onRetry`/`retryLabel` to the local `ErrorNotice` component and wired `reload` into it for both surfaces — `BusinessPartnerRequestDetail` already had a `reload` callback; `BusinessPartnerRequestEdit` didn't (its fetch was inline in a `useEffect`), so it was extracted into a `useCallback` and reused as both the effect body and the retry handler, clearing `error`/`errorState` at the start so a retry visibly re-enters the loading state rather than continuing to show the stale error. This is a genuine, small behavior addition (a working Retry button where none existed), not a pure refactor — called out explicitly. Verified: typecheck clean for `@athyper/platform-shell` and the Business Partner package, 225/225 tests pass. `BusinessPartnerRequestList`/`BusinessPartnerAggregateDetail` were not touched — their loading/error shapes are inline notices beside ready content, not a full-page state swap, so they don't fit this same boundary shape without a different design; left for a later pass rather than forced.
- [x] Adapt collection and detail/360 to PageWorkspace and the generic runtimes — investigated first, given this was flagged as the larger item. Findings: `BusinessPartner360Shell` already delegates to a generic, already-sophisticated shared component (`EntityRecord360Panel` in `packages/platform/entity/runtime/form-detail/src/record-360-panel.tsx`) with its own accessible keyboard-navigable tab strip, progressive/lazy section loading via `IntersectionObserver`, and scroll-linked section tracking. It renders a plain `<div>`, not `<main>`, so it does not have the duplicate-landmark bug found and fixed earlier in `index.tsx` — collection/360 were not carrying that particular defect. What it did have: the identical loading/error branching gap just fixed elsewhere (no `aria-busy`, error state not routed through a shared boundary, though it already had a working "Try again" retry unlike the other two surfaces). Fixed that specific piece: wired `BusinessPartner360Shell`'s existing error/loading/ready branches through `PageResourceBoundary`, preserving the existing retry button and guard conditions exactly (kept the literal `if (!summary || ...)` check as the return gate rather than an intermediate status variable, to avoid the type-narrowing loss hit in the previous round). This required updating `record-opening.test.tsx`'s manual `vi.mock("@athyper/platform-shell", ...)` to add a passthrough `PageResourceBoundary` mock — caught immediately by the test suite (typecheck alone did not catch it), fixed, re-verified. Collection view separately investigated and confirmed clean (see below). Completed the deferred design pass on reusing `PageNavigation` for `EntityRecord360Panel`'s tab strip, rather than leaving it open indefinitely: confirmed the tab strip renders exactly one shared `role="tabpanel"` content div (`${id}-content`), not per-tab panels — a fundamentally different content-mounting model than `platform-ui`'s `Tabs`/`TabsContent`, which keeps every panel mounted and toggles `hidden`. That difference isn't incidental: the "360" tab's `useEntitySectionScroll` (scroll-linked progressive loading across multiple sections) assumes it's the only rendered content, re-initializing via the `enabled: tab.provider === "360"` guard and `scopeKey`. Forcing the always-mounted-panel model here would mean restructuring that working, tested scroll/progressive-loading logic to accommodate an abstraction mismatch, not a genuine simplification. Also checked the hand-rolled keyboard handler (ArrowLeft/ArrowRight/Home/End, roving `tabIndex`) against `TabsList`'s reference implementation — functionally equivalent and correctly WAI-ARIA compliant; the one minor difference (`TabsList` filters `:not([disabled])` when cycling, this doesn't) is latent, not live, since no tab here is ever `disabled` today. **Decision: do not adopt `PageNavigation` here** — this is a legitimate Level-4 "custom content workspace" per system-design.md §14.4, correctly specialized because its content-mounting requirements genuinely differ, not because of duplicated effort. Added a short code comment explaining this so a future reader doesn't wonder why it doesn't reuse the shared `Tabs` primitives. Verified: typecheck clean for `@athyper/platform-entity-form-detail`, 225/225 Business Partner tests pass, both 360-panel/section-layout browser specs pass. This closes out the last open item in Task 5.
- [x] Investigated the collection view itself: `NeonEntityApplicationSection` → `EntityApplicationSection` is a thin dispatcher (no header of its own, defers entirely, `contentOnly` on `EntityListRuntime`) into `packages/platform/entity/runtime/list-view/src/index.tsx` — a 5,000+ line platform file already using `PageFrame`/`PageHeader` in multiple places, shared across every entity's collection view on every plane, not scoped to Business Partner. No concrete defect found (no duplicate landmark, no obviously missing slot). Deliberately not touched: auditing a file at that scale and blast radius without a specific, evidenced finding pulling toward a fix would be exactly the speculative work this plan says to avoid. Confirmed clean; left alone.
- [x] Adapt intake while preserving steps, draft values, validation and governed submission. Submit is not necessarily direct record creation. Findings: intake is substantially more mature than assumed — a full shared `EntityIntake`/`useEntityIntake`/`IntakeController` system already exists (`packages/platform/entity/runtime/form-detail/src/intake.tsx`, 448 lines) handling step navigation, dirty/save state, and header sharing via the same `useRegisterEntityTaskHeader` mechanism used elsewhere. Checked the actual submit logic in `NewBusinessPartnerRequest`: it already correctly treats submission as a distinct governed operation (`submitBusinessPartnerIntake`) separate from creating the draft record — the "submit is not necessarily direct record creation" principle already holds, not something to build. One concrete gap found and fixed: the initial published-form load had no retry — if it failed, the only recovery was a full page reload. Extracted the fetch (previously inline in a `useEffect`) into a `loadPublishedForm` callback reused as both the effect body and a new `onRetry` on the existing `ErrorNotice`. Verified: typecheck clean, 225/225 tests pass.
- [x] Investigated Studio's composition editor before wrapping anything. Findings: every Studio Business Partner route (`model`, `workflows`, `validation`, `matching`, `publication`, `operations`, `ai-experience`, and the module overview) already wraps its content in `PageFrame`/`PageHeader` correctly at the route level — a cleaner baseline than the Neon plane had before this round's fixes; no duplicate-landmark bug here at all. None of `workbench.tsx`/`composition-workspace.tsx`/`composition-navigation.tsx`/`composition-editor.tsx`/`composition-review.tsx` (2,691 lines total) render `PageFrame`/`PageWorkspace` themselves, which is correct — they're the content, not the frame. One concrete gap found and fixed: in `compositionMode` (the actual embedded editor context used by Studio), a load error had no recovery path at all — the existing "Refresh stored data" retry button sits inside a `hidden={compositionMode}` div, so it's simply unreachable in composition mode. Added a `Retry` button next to the `compositionMode && visible.error` message, reusing the exact same `mayLeave()`-guarded `setAttempt` mechanism the existing (but compositionMode-hidden) button already uses — no new state, no new mechanism. Verified: typecheck clean, 78/78 existing tests pass. The bigger ask — a reusable `ConfigurationRuntime` wrapper (draft/revision context integration, view navigation, dirty/save integration, validation-result presentation, per system-design §14.5) — was deliberately not attempted: there is exactly one consumer today, so designing that wrapper now would be the same kind of speculative, unproven-by-a-second-consumer work already avoided for `PageNavigation`'s route-links/workflow-steps kinds. Needs its own scoping conversation, not a rushed build.
- [x] Keep specialized qualifications, duplicate checks and other domain logic in providers — audited, not just assumed. `duplicateCheck` is the only such term found in shared platform code (`packages/platform/entity/runtime/form-detail/src/collection-section.tsx`), and it's generic, declarative metadata support defined in `packages/contracts/platform/entity-runtime` (any entity's descriptor can declare one) — not hardcoded Business Partner logic. Actual BP-specific qualification/duplicate-check logic lives only in the BP plane package, as it should. The two shared components built this session (`page-navigation.tsx`, `page-resource-boundary.tsx`) mention Business Partner only in explanatory comments, never in logic. No violations found.
- [x] Replace one experience at a time / preserve route/query behavior, permissions and existing business operations — verified against the actual diffs, not recalled from memory. Every relocated line across this session's changes (extracting `commandBar`, moving hrefs into `PageNavigation.items[].content`, moving the command bar into a local) matched its original content exactly in a diff review; no route, permission check (`governedCaseActions`, `actions.includes(...)`, `permissions.has(...)`), or href value was altered, only repositioned.
- [x] Investigated the deferred cross-plane observation from the previous round (Neon's `useGuardedNavigation` vs. Studio's unsaved-changes guard) properly rather than leaving it as a footnote. Correction to an earlier claim: Studio does have `beforeunload` protection — it just lives in `composition-editor.tsx` and `workbench-editor.tsx`, not in `workbench.tsx`'s `mayLeave()`, which I'd checked instead. Those two files turned out to duplicate nearly identical logic (`beforeunload` handler plus a document-level capturing click-interceptor showing `window.confirm(...)`, differing only in message text) — real, verified, same-package duplication, not a speculative generalization. Extracted into `packages/planes/studio/business-partner/src/unsaved-changes-guard.ts` (`useUnsavedChangesGuard(dirty, busy, message)`), kept local to the Studio Business Partner plane package since both consumers are there — not promoted to a platform-shared package without a consumer outside this plane. One editing mistake caught before it shipped: the first extraction attempt in `composition-editor.tsx` left a dangling orphaned `}, [dirty, busy]);` line from an incomplete match; found by re-reading the file immediately after editing, fixed, then verified. Left as a separate, explicitly deferred decision: Neon's pattern uses the app's own accessible `ConfirmDialog`, while both Studio implementations use a native, unstyled `window.confirm` — a real accessibility/consistency gap on what's arguably the highest-stakes authoring surface in the app, but fixing it means migrating Studio's DOM-click-interception model to the dialog component, which is bigger than this extraction. Verified: typecheck clean, 78/78 tests pass.

### 6. Prove reuse with another entity and Mesh

- [x] Pick a second entity with different fields/relationships; only add page kinds it needs. **Resolved as a structural proof, not a real migration — stated plainly.** Confirmed real backend descriptor registration isn't feasible right now: no `ATHYPER_NEON_DATABASE_ADMIN_URL` configured, no local Postgres client available, no docker-compose database service found. The actual descriptor-registration mechanism (`server/db/scripts/seed/apply-meta-entity-seed-pack.ts`, writing meta-entity graphs via raw SQL against a real database) is real infrastructure work, out of scope here without deliberately setting it up. Picked `currency` as the model entity for the structural proof: the simplest real backend table found (`server/db/.../types.ts`: `id, code, name, symbol, minor_units, numeric3, metadata, status, is_active` — no hierarchy, no company/tenant scoping, no relationships), already recognized as a first-class "reference domain" on the backend (`server/packages/platform/reference-data`, alongside `country`/`uom`/`locale`) even though it has no entity descriptor yet. Matches system-design.md §14.4's own canonical example for "Level 1 — Metadata configuration: a simple reference entity" almost exactly. Built `tests/foundation/second-entity-reuse-currency.test.tsx`: a synthetic `CurrencyDetailPage` composing the real, unmodified `PageWorkspace`/`PageLayout`/`PageResourceBoundary` (the same components used across Business Partner) against a hardcoded currency fixture, asserting loading/error/empty/ready render correctly and exactly one `<h1>` exists regardless of entity. The test's docblock states explicitly that `currency` is not a registered entity and this proves nothing about a real backend or publication pipeline. **The test caught a real bug while being written**, not a hypothetical one: `page-navigation.tsx` and `page-resource-boundary.tsx` (built earlier this session) were missing the explicit `import React` that every other file in the same directory (`page-workspace.tsx`) has — worked fine under vitest's automatic JSX runtime, but broke with `ReferenceError: React is not defined` under this repo's `tsx --tsconfig tooling/config/tsconfig-react.json` foundation-test runner, which resolves JSX differently. Fixed both to match the established convention; re-verified typecheck clean for `@athyper/platform-shell` plus the Neon/Studio/Mesh Business Partner packages (all still 0 errors), and the new test passes.
- [x] Render ordinary sections through metadata and existing providers — demonstrated within the structural proof's scope: the `currency` fixture's fields render through the same generic `PageLayout` used for real entities, no entity-specific rendering code added.
- [x] Add one specialized provider if the entity needs it — not needed: `currency` is flat reference data with no relationships or governance workflow, so zero specialized providers were required, matching "only add page kinds/providers it needs" rather than forcing unnecessary custom code for the sake of the exercise.
- [x] Check a representative Mesh page with network-account context. Investigated all four functions in `packages/planes/mesh/business-partner/src/index.tsx` individually rather than assuming a gap: `BusinessPartnerOverview`/`BusinessPartnerProfile` are derived-only from `useMeshAccountContext()`, which already handles its own loading state — nothing to fix. `BusinessPartnerChangeRequests`/`BankDisclosureControl` are one-shot mutation forms where resubmitting already serves as retry — nothing to fix. `NetworkExchangeWorkspace` (relationship/capability/registration-exchange lists) had a real, confirmed gap: no loading state at all for its initial workspace fetch — the lists just rendered empty with no way to distinguish "still loading" from "genuinely empty" (conflating exactly the states invariant #6 says must stay distinct), and its error message was a bare `<p role="alert">` at the bottom of the page with no retry, despite an already-existing `load()` function that could serve as one. Added a `workspaceLoading` state (cleared alongside `error` at the start of `load()`, matching the same reset discipline used earlier this session for `BusinessPartnerRequestEdit`'s retry), a `role="status" aria-busy="true"` loading message shown only while loading and before any data has arrived, and a Retry button next to the error message calling the same `load()`. This package has no test suite at all (only a `typecheck` script) — the first package touched this session without one — so verification here is typecheck-clean plus a careful manual diff review, not test-verified. Confirms the shared discipline (investigate before fixing, verify what's actually there) generalizes to a different plane's patterns (network accounts vs. operating organizations), independent of the second-entity question above.
- [ ] Confirm there are no new copies of Main, headers, boundaries, scrolling or footer logic and no entity branches in the resolver. — partially evidenced: the fully generic catalog route (`apps/neon/app/(shell)/[workspaceSlug]/[moduleSlug]/[[...segments]]/page.tsx`) has zero entity-specific code by construction, resolving any registered entity through `EntityDetailRuntime`/`EntityFormRuntime`/`EntityApplicationLayout`; and the `currency` structural proof adds direct evidence that `PageWorkspace`/`PageLayout`/`PageResourceBoundary` render correctly, unmodified, for a differently-shaped entity (one `<h1>` regardless). Still not full confirmation — that requires an actual registered entity flowing through the real resolver end to end, which the structural proof deliberately does not attempt.

Extension choice: metadata only → existing provider composition → specialized provider → custom content workspace. Use the smallest sufficient level; even custom workspaces retain shared page conventions.

### 7. Finish shared utilities and Knowledge as needed

- [ ] Reuse System/Light/Dark preferences across planes; add Compact density after spacing tokens are consistent.
- [ ] Share Language, Help and About controls. Show the actual app build/release information separately from record/metadata revisions.
- [ ] Start Knowledge with supported read-only Wiki/Documents browsing, authorized search, preview and links.
- [ ] Show filtered shared Recent/Favorites rather than introducing new stores.
- [ ] Add authoring/upload/Atlas integrations only when their backend capability is actually available.

### 8. Remove replaced code and finish

- [ ] Move reusable route-local code into the right existing package without mass renaming.
- [ ] Remove verified duplicate components, CSS, exports and unused dependencies after replacement works.
- [ ] Update affected imports, manifests, asset references and existing generated ownership data using its generator when necessary.
- [ ] Run relevant existing typechecks/tests and affected app builds.
- [ ] Update these documents to describe what was actually built and what remains unfinished.

## Lightweight verification

After each meaningful change, check the affected behavior rather than running an exhaustive release matrix:

- Load the page and try its normal task.
- Check a narrow window and keyboard navigation; use zoom when changing sticky geometry or text layout. Keep existing RTL support intact; a new exhaustive RTL matrix is not required.
- Exercise loading, one failure/retry, empty data where applicable, and a denied-access case when touching access integration.
- Check tab/section navigation, Back/Forward and unsaved input when changing those areas.
- For context changes, verify previous-context data disappears and footer information updates.
- Run affected package typechecks and existing relevant tests. Add tests for real regressions or new validation behavior, not every low-impact extraction.
- Use existing dependency and contract checks when touching those boundaries; do not introduce another enforcement framework for this cleanup.

Existing starting points include shared-shell/record-navigation browser tests, Business Partner section/360/intake/editor tests, policy boundary scripts and OpenAPI checks. Inspect their current setup before running them. No test result is implied by this list.

## Later, when deployment requires it

Keep three independently buildable apps and avoid hard-coded hostnames or new process-local authoritative state. Preserve existing session/storage integrations. Do not build replica caches, compatibility negotiation, cohort rollouts or cross-version release infrastructure now. Revisit them when moving beyond local development or supporting multiple independently deployed versions.

## Done for this local build

The Business Partner experiences and another entity use the shared foundation, Mesh can reuse it, the touched flows work locally, relevant checks pass, and replaced code is removed without losing domain behavior. Mark the task checkboxes as work is completed; no named-owner sign-off or document semver is required. Current checkboxes describe planned work, not completed implementation.
