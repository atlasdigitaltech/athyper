# BP-AI-06 local implementation evidence — 2026-09-09

Scope: source implementation and synthetic local qualification. No deployment, authenticated persona/browser run, live model evaluation or production-scale database benchmark was performed.

| Check | Result |
| --- | --- |
| Platform AI package tests | 307 passed |
| BP-AI-06 focused tool tests (included in package suite) | 16 passed; includes a 100,000-candidate synthetic population |
| Host Records parity and AI composition tests | 13 passed; real in-memory Records list/query services over 2,000 partners |
| Records package tests | 206 passed, 3 skipped by the existing suite |
| Atlas answer UI tests | 8 passed |
| Existing `qualify:business-partner-r9` | Passed: 54 AI, 37 master-data, 3 provisioning tests |
| AI, platform host and shell typechecks | Passed |
| Whitespace validation | `git diff --check` passed |

Tests cover bounded reads, distinct and overlapping issue counts, explicit targets, visible-page membership, authorization/count parity, query-field denial, tenant isolation, approximate-count omission, provider/restricted states, cancellation, time-budget admission, replay reauthorization and escaped accessible comparison rendering. Existing tests and R9 results overlap; do not sum these counts as independent coverage.

The list capability is registered with the existing NEON insight owner and read controls. Profile allowlists remain authoritative. Results examine at most 20 partners, retain at most 100 evidence entries and enforce a 32 KiB tool result ceiling. Exact totals are requested from Records. The 3.5-second owner-read admission budget and 5-second tool deadline do not establish live latency targets. Current reads do not provide a cross-service snapshot.

See [the implementation contract](../../../contracts/atlas-business-partner-list-insights.md) for coverage meanings and pending release qualification.
