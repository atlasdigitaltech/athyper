name: Brand Sync Check

on:
  pull_request:
    paths:
      - "packages/shared/foundation/brand/**"
      - "packages/platform/foundation/brand/assets/athyper-favicon.png"
      - "packages/platform/foundation/brand/src/**"
      - "packages/platform/foundation/theme/src/**"
      - "packages/platform/foundation/theme/scripts/build-iam-css.ts"
      - "apps/*/public/brand/*/favicon.png"
      - "stack/config/iam/themes/neon/login/_neon-brand-*.ftl"
      - "stack/config/iam/themes/neon/login/resources/img/neon-*.svg"
      - "stack/config/iam/themes/neon/login/**/*.ftl"
      - "stack/config/iam/themes/neon/login/resources/img/athyper-favicon.png"
      - "stack/config/gateway/fallback/brand/athyper-favicon.png"
      - "deploy/compose/*/**/status.html"
      - "scripts/brand/**"
      - ".github/workflows/brand-sync.yml"
  push:
    branches: [main, develop]

jobs:
  verify-brand-sync:
    name: Verify KC brand assets match canonical source
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24.19.0
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify universal favicon sync
        run: pnpm brand:check:favicon

      - name: Verify Keycloak brand sync
        run: pnpm brand:verify
