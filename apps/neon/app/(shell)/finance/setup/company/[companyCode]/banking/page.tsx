import{redirect}from"next/navigation";import{CompanyBankingTreasuryView}from"@athyper/finance-workbench";import{getNeonServerSession}from"@/lib/server/session";export default async function BankingPage({params}:{params:Promise<{companyCode:string}>}){const{companyCode}=await params,decoded=decodeURIComponent(companyCode);if(!await getNeonServerSession())redirect(`/login?next=${encodeURIComponent(`/finance/setup/company/${decoded}/banking`)}`);return <CompanyBankingTreasuryView companyCode={decoded}/>}
