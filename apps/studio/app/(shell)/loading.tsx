import { AppLoadingBoundary } from "@athyper/platform-shell-app-foundation";
export default function ShellLoading() { return <AppLoadingBoundary kind="refresh" label="Updating Studio workspace" />; }
