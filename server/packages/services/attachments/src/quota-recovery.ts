import type{JobExecutionResult,JobHandler}from"@athyper/server-contract-jobs";import type{PlaneTransactionCoordinator}from"@athyper/server-foundation/transaction";import type{AttachmentQuotaLedger}from"./quota.js";import type{AttachmentRepository}from"./attachment-lifecycle.js";
export const ATTACHMENT_MAINTENANCE_QUEUE="attachments.maintenance",EXPIRE_ATTACHMENT_RESERVATIONS_JOB="attachments.expire-reservations";
export interface ExpireAttachmentReservationsRequest{readonly planeKey:"studio"|"neon"|"mesh";readonly tenantId:string;readonly principalId:string;readonly batchSize?:number;readonly[key:string]:unknown;}
export function createAttachmentQuotaRecoveryHandler<T>(input:{transactions:PlaneTransactionCoordinator<T>;quota:AttachmentQuotaLedger<T>;attachments:AttachmentRepository<T>}):JobHandler<typeof EXPIRE_ATTACHMENT_RESERVATIONS_JOB,ExpireAttachmentReservationsRequest>{return{async handle(job):Promise<JobExecutionResult>{const value=job.data,limit=Math.min(500,Math.max(1,value.batchSize??100));const expired=await input.transactions.run(value.planeKey,value,async tx=>{const ids=await input.quota.expire({tenantId:value.tenantId,principalId:value.principalId,limit},tx);for(const attachmentId of ids)await input.attachments.expire({...value,attachmentId},tx);return ids;});return{status:"completed",output:{expired:expired.length}};}};}
