import {randomUUID}from"node:crypto";import {sql,type Transaction} from "kysely";
import type {AttachmentRecord,AttachmentRepository} from "./attachment-lifecycle.js";

export type AttachmentTransaction=Transaction<Record<string,never>>;
interface Row{ id:string;status:AttachmentRecord["status"];file_name:string;content_type:string|null;text_extraction_status:string|null;storage_key:string;size_bytes:string|number|null;sha256:string|null;metadata:Record<string,unknown>|string;is_current:boolean;is_active:boolean;expires_at:Date|string|null;retention_until:Date|string|null;series_id:string;has_legal_hold:boolean; }

export function createKyselyAttachmentRepository(storageBucket:string):AttachmentRepository<AttachmentTransaction>{if(!storageBucket.trim())throw new TypeError("Attachment storage bucket is required");return{
  async lockForStaging(identity,tx){await sql`SELECT pg_advisory_xact_lock(hashtextextended(${`${identity.tenantId}:${identity.attachmentId}`},0))`.execute(tx);},
  async createStaged(input,tx){let parent:{id:string;version_no:number;series_id:string}|undefined;let seriesId=input.seriesId??randomUUID();if(input.parentAttachmentId){const loaded=await sql<{id:string;version_no:number;series_id:string}>`SELECT id,version_no,series_id FROM document.attachment WHERE tenant_id=${input.tenantId}::uuid AND id=${input.parentAttachmentId}::uuid AND is_active FOR UPDATE`.execute(tx);parent=loaded.rows[0];if(!parent)throw new Error("Parent attachment not found");if(input.seriesId&&parent.series_id!==input.seriesId)throw new Error("Attachment series mismatch");seriesId=parent.series_id;}await sql`INSERT INTO document.attachment_series(id,tenant_id,created_by) VALUES(${seriesId}::uuid,${input.tenantId}::uuid,${input.principalId}::uuid) ON CONFLICT(tenant_id,id) DO NOTHING`.execute(tx);
    const result=await sql<Row>`INSERT INTO document.attachment(id,tenant_id,file_name,original_filename,content_type,size_bytes,kind,storage_bucket,storage_key,version_no,parent_attachment_id,series_id,is_active,is_virus_scanned,expires_at,uploaded_by,metadata,status,created_by) VALUES(${input.attachmentId}::uuid,${input.tenantId}::uuid,${input.fileName},${input.fileName},${input.contentType},${input.sizeBytes??null},'attachment',${storageBucket},${input.storageKey},${parent?parent.version_no+1:1},${parent?.id??null}::uuid,${seriesId}::uuid,true,false,${input.expiresAt}::timestamptz,${input.principalId}::uuid,${JSON.stringify({staged_size_bytes:input.sizeBytes??null,provenance:input.provenance??{source:"user_upload"},...(input.entityType?{entity_type:input.entityType}:{}),...(input.entityId?{entity_id:input.entityId}:{})})}::jsonb,'uploading',${input.principalId}::uuid) RETURNING *,false AS is_current,false AS has_legal_hold`.execute(tx);const row=required(result.rows[0]);if(input.entityType&&input.entityId)await sql`INSERT INTO document.attachment_link(tenant_id,entity_type,entity_id,attachment_series_id,link_kind,created_by) VALUES(${input.tenantId}::uuid,${input.entityType},${input.entityId},${seriesId}::uuid,'context',${input.principalId}::uuid) ON CONFLICT(tenant_id,entity_type,entity_id,attachment_series_id,pinned_attachment_id,link_kind) DO NOTHING`.execute(tx);return map(row);},
  async load(identity,tx){const result=await sql<Row>`SELECT attachment.*,series.current_attachment_id=attachment.id AS is_current,EXISTS(SELECT 1 FROM document.attachment_legal_hold hold WHERE hold.tenant_id=attachment.tenant_id AND hold.attachment_series_id=attachment.series_id AND hold.released_at IS NULL) AS has_legal_hold FROM document.attachment attachment JOIN document.attachment_series series ON series.tenant_id=attachment.tenant_id AND series.id=attachment.series_id WHERE attachment.tenant_id=${identity.tenantId}::uuid AND attachment.id=${identity.attachmentId}::uuid AND attachment.uploaded_by=${identity.principalId}::uuid LIMIT 1`.execute(tx);return result.rows[0]?map(result.rows[0]):null;},
  async loadForDownload(identity,tx){const result=await sql<Row>`SELECT attachment.*,series.current_attachment_id=attachment.id AS is_current,EXISTS(SELECT 1 FROM document.attachment_legal_hold hold WHERE hold.tenant_id=attachment.tenant_id AND hold.attachment_series_id=attachment.series_id AND hold.released_at IS NULL) AS has_legal_hold FROM document.attachment attachment JOIN document.attachment_series series ON series.tenant_id=attachment.tenant_id AND series.id=attachment.series_id WHERE attachment.tenant_id=${identity.tenantId}::uuid AND attachment.id=${identity.attachmentId}::uuid LIMIT 1`.execute(tx);return result.rows[0]?map(result.rows[0]):null;},
  async loadForMaintenance(identity,tx){const result=await sql<Row>`SELECT attachment.*,series.current_attachment_id=attachment.id AS is_current,EXISTS(SELECT 1 FROM document.attachment_legal_hold hold WHERE hold.tenant_id=attachment.tenant_id AND hold.attachment_series_id=attachment.series_id AND hold.released_at IS NULL) AS has_legal_hold FROM document.attachment attachment JOIN document.attachment_series series ON series.tenant_id=attachment.tenant_id AND series.id=attachment.series_id WHERE attachment.tenant_id=${identity.tenantId}::uuid AND attachment.id=${identity.attachmentId}::uuid LIMIT 1`.execute(tx);return result.rows[0]?map(result.rows[0]):null;},
  async finalizeClean(identity,input,tx){const result=await sql<Row>`UPDATE document.attachment SET storage_key=${input.storageKey},sha256=${input.sha256},size_bytes=${input.sizeBytes},content_type=${input.contentType},status='active',is_active=true,is_virus_scanned=true,expires_at=NULL,status_changed_at=clock_timestamp(),status_changed_by=${identity.principalId}::uuid,updated_by=${identity.principalId}::uuid WHERE tenant_id=${identity.tenantId}::uuid AND id=${identity.attachmentId}::uuid AND uploaded_by=${identity.principalId}::uuid AND status IN('pending','uploading','uploaded') RETURNING *,false AS is_current,false AS has_legal_hold`.execute(tx);const row=required(result.rows[0]);await sql`UPDATE document.attachment_series SET current_attachment_id=${identity.attachmentId}::uuid,updated_at=clock_timestamp(),updated_by=${identity.principalId}::uuid WHERE tenant_id=${identity.tenantId}::uuid AND id=${row.series_id}::uuid`.execute(tx);return map({...row,is_current:true});},
  async quarantine(identity,reason,tx){await sql`UPDATE document.attachment SET status='quarantined',is_active=false,metadata=metadata||${JSON.stringify({quarantine_reason:reason})}::jsonb,status_changed_at=clock_timestamp(),status_changed_by=${identity.principalId}::uuid,updated_by=${identity.principalId}::uuid WHERE tenant_id=${identity.tenantId}::uuid AND id=${identity.attachmentId}::uuid`.execute(tx);},
  async deactivate(identity,reason,tx){await sql`UPDATE document.attachment SET status='deleted',is_active=false,metadata=metadata||${JSON.stringify({deactivation_reason:reason})}::jsonb,status_changed_at=clock_timestamp(),status_changed_by=${identity.principalId}::uuid,updated_by=${identity.principalId}::uuid WHERE tenant_id=${identity.tenantId}::uuid AND id=${identity.attachmentId}::uuid AND uploaded_by=${identity.principalId}::uuid`.execute(tx);},
  async expire(identity,tx){await sql`UPDATE document.attachment SET status='expired',is_active=false,status_changed_at=clock_timestamp(),status_changed_by=${identity.principalId}::uuid,updated_by=${identity.principalId}::uuid WHERE tenant_id=${identity.tenantId}::uuid AND id=${identity.attachmentId}::uuid`.execute(tx);},
  async markPurged(identity,tx){await sql`UPDATE document.attachment SET status='deleted',is_active=false,storage_key='purged/'||id::text,status_changed_at=clock_timestamp(),status_changed_by=${identity.principalId}::uuid,updated_by=${identity.principalId}::uuid WHERE tenant_id=${identity.tenantId}::uuid AND id=${identity.attachmentId}::uuid`.execute(tx);},
  async markPurgedForMaintenance(identity,tx){await sql`UPDATE document.attachment SET status='deleted',is_active=false,storage_key='purged/'||id::text,status_changed_at=clock_timestamp(),status_changed_by=${identity.principalId}::uuid,updated_by=${identity.principalId}::uuid WHERE tenant_id=${identity.tenantId}::uuid AND id=${identity.attachmentId}::uuid AND status IN('expired','deleted','orphaned')`.execute(tx);},
  async listRetentionCandidates(input,tx){const result=await sql<{id:string}>`SELECT attachment.id FROM document.attachment attachment WHERE attachment.tenant_id=${input.tenantId}::uuid AND attachment.status IN('expired','deleted','orphaned') AND COALESCE(attachment.retention_until,'-infinity'::timestamptz)<=${input.before}::timestamptz AND NOT EXISTS(SELECT 1 FROM document.attachment_legal_hold hold WHERE hold.tenant_id=attachment.tenant_id AND hold.attachment_series_id=attachment.series_id AND hold.released_at IS NULL) ORDER BY attachment.status_changed_at NULLS LAST,attachment.created_at LIMIT ${input.limit} FOR UPDATE SKIP LOCKED`.execute(tx);return result.rows.map(row=>row.id);},
};}
function map(row:Row):AttachmentRecord{const metadata=typeof row.metadata==="string"?JSON.parse(row.metadata) as Record<string,unknown>:row.metadata;const provenance=metadata["provenance"];return{...(typeof metadata["entity_type"]==="string"?{entityType:metadata["entity_type"]}:{}),...(typeof metadata["entity_id"]==="string"?{entityId:metadata["entity_id"]}:{}),id:row.id,status:row.status,fileName:row.file_name,...(row.content_type?{contentType:row.content_type}:{}),textExtractionStatus:row.text_extraction_status,storageKey:row.storage_key,...(row.size_bytes!==null?{sizeBytes:Number(row.size_bytes)}:{}),...(row.sha256?{sha256:row.sha256}:{}),...(provenance&&typeof provenance==="object"?{provenance:provenance as AttachmentRecord["provenance"]}:{}),isCurrent:row.is_current,isActive:row.is_active,hasLegalHold:Boolean(row.has_legal_hold),...(row.expires_at?{expiresAt:iso(row.expires_at)}:{}),...(row.retention_until?{retentionUntil:iso(row.retention_until)}:{}),seriesId:row.series_id};}
function required(row:Row|undefined):Row{if(!row)throw new Error("Attachment persistence conflict");return row;}
function iso(value:Date|string):string{return value instanceof Date?value.toISOString():new Date(value).toISOString();}
