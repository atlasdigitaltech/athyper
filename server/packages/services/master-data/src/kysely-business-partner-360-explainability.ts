import{sql,type Transaction}from"kysely";import type{BusinessPartner360EvidenceManifestItem,BusinessPartner360MaterializationLink,BusinessPartner360RequestHistoryItem}from"@athyper/server-contract-master-data";import type{BusinessPartner360ExplainabilityRead,BusinessPartner360Repository}from"./business-partner-360-service.js";import{mapBusinessPartner360Activity,type BusinessPartner360RawActivity}from"./business-partner-360-activity-mapper.js";
type Tx=Transaction<Record<string,never>>;type Row=Record<string,unknown>;type Input=Parameters<BusinessPartner360Repository<Tx>["readExplainabilitySection"]>[0];
export async function readBusinessPartner360ExplainabilitySection(input:Input,tx:Tx):Promise<BusinessPartner360ExplainabilityRead>{return input.sectionCode==="requests"?requests(input,tx):activity(input,tx);}
async function requests(input:Input,tx:Tx){const pageSize=Math.max(1,input.limit-1),[rows,counts]=await Promise.all([sql<Row>`SELECT request.id::text,request.request_no,request.request_kind::text,request.source_kind,request.requested_role::text,request.status,request.workflow_request_id::text,request.created_at,request.created_by::text,request.submitted_at,request.submitted_by::text,request.approved_at,request.approved_by::text,request.applied_at,request.applied_by::text,request.application_result_kind,request.application_reason_code,COALESCE(evidence.items,'[]'::jsonb) evidence,COALESCE(materialization.items,'[]'::jsonb) materialization FROM document.business_partner_request request LEFT JOIN LATERAL(SELECT jsonb_agg(jsonb_build_object('id',item.id,'kind',item.evidence_kind,'classification',item.classification_code,'verificationStatus',item.verification_status,'attachmentId',item.attachment_id,'snapshotId',item.snapshot_id,'createdAt',item.created_at)ORDER BY item.created_at,item.id)items FROM document.business_partner_request_evidence item WHERE item.tenant_id=request.tenant_id AND item.request_id=request.id)evidence ON true LEFT JOIN LATERAL(SELECT jsonb_agg(jsonb_build_object('id',item.id,'childKind',item.child_kind,'definitionFieldCode',item.definition_field_code,'targetType',item.target_table,'targetId',item.target_id,'appliedAt',item.applied_at)ORDER BY item.applied_at,item.id)items FROM document.business_partner_request_materialization_item item WHERE item.tenant_id=request.tenant_id AND item.request_id=request.id)materialization ON true WHERE request.tenant_id=${input.tenantId}::uuid AND(request.target_business_partner_id=${input.businessPartnerId}::uuid OR request.materialized_business_partner_id=${input.businessPartnerId}::uuid)AND request.created_at<=${input.cursor.snapshotAt}::timestamptz ${after(input,"request.created_at","request.id","request")} ORDER BY request.created_at DESC,request.id DESC LIMIT ${pageSize+1}`.execute(tx),sql<Row>`SELECT count(*)FILTER(WHERE status IN('draft','validating','validation_failed','pending_approval','returned','approved','applying'))::int active,count(*)FILTER(WHERE status='returned')::int returned,count(*)FILTER(WHERE status='pending_approval')::int pending_approval,count(*)FILTER(WHERE status='approved')::int approved,count(*)FILTER(WHERE status='failed')::int failed FROM document.business_partner_request WHERE tenant_id=${input.tenantId}::uuid AND(target_business_partner_id=${input.businessPartnerId}::uuid OR materialized_business_partner_id=${input.businessPartnerId}::uuid)`.execute(tx)]),page=rows.rows.slice(0,pageSize),last=page.at(-1),items=page.map(mapRequest);return{items,summary:{active:Number(counts.rows[0]?.["active"]??0),returned:Number(counts.rows[0]?.["returned"]??0),pendingApproval:Number(counts.rows[0]?.["pending_approval"]??0),approved:Number(counts.rows[0]?.["approved"]??0),failed:Number(counts.rows[0]?.["failed"]??0)},...(rows.rows.length>pageSize&&last?{next:{at:iso(last["created_at"]),id:text(last,"id"),source:"request"}}:{}),provenance:[provenance("document.business_partner_request",input.cursor.snapshotAt)]};}
async function activity(input:Input,tx:Tx){const pageSize=Math.max(1,input.limit-1),rows=(await sql<Row>`SELECT audit.id::text,audit.event_code,audit.occurred_at,audit.actor_principal_id::text actor_id,'audit' source,audit.source_service,audit.request_id,audit.entity_type,audit.entity_id::text,audit.outcome::text,audit.changed_fields FROM audit.audit_log audit WHERE audit.tenant_id=${input.tenantId}::uuid AND audit.occurred_at<=${input.cursor.snapshotAt}::timestamptz AND audit.event_code LIKE 'business_partner.%' AND(audit.entity_type='business_partner' AND audit.entity_id=${input.businessPartnerId}::uuid OR audit.context->>'businessPartnerId'=${input.businessPartnerId} OR audit.entity_type='business_partner_request'AND EXISTS(SELECT 1 FROM document.business_partner_request request WHERE request.tenant_id=audit.tenant_id AND request.id=audit.entity_id AND(request.target_business_partner_id=${input.businessPartnerId}::uuid OR request.materialized_business_partner_id=${input.businessPartnerId}::uuid)))${after(input,"audit.occurred_at","audit.id","audit")} ORDER BY audit.occurred_at DESC,audit.id DESC,'audit' DESC LIMIT ${pageSize+1}`.execute(tx)).rows,page=rows.slice(0,pageSize),last=page.at(-1),items=page.map(row=>mapBusinessPartner360Activity({id:text(row,"id"),eventCode:text(row,"event_code"),occurredAt:iso(row["occurred_at"]),...(optional(row,"actor_id")?{actorId:optional(row,"actor_id")} : {}),source:"audit",sourceService:text(row,"source_service"),...(optional(row,"request_id")?{requestId:optional(row,"request_id")} : {}),entityType:text(row,"entity_type"),...(optional(row,"entity_id")?{entityId:optional(row,"entity_id")} : {}),...(optional(row,"outcome")?{outcome:optional(row,"outcome")} : {}),changedFields:stringArray(row["changed_fields"]),evidenceHref:evidenceHref(row,input.businessPartnerId)} satisfies BusinessPartner360RawActivity));return{items,...(rows.length>pageSize&&last?{next:{at:iso(last["occurred_at"]),id:text(last,"id"),source:"audit"}}:{}),provenance:[provenance("audit.audit_log",input.cursor.snapshotAt)]};}
function mapRequest(row:Row):BusinessPartner360RequestHistoryItem{const id=text(row,"id"),evidence=jsonArray(row["evidence"]).map(item=>({id:String(item["id"]),kind:String(item["kind"]),classification:String(item["classification"]),verificationStatus:String(item["verificationStatus"]),...(item["attachmentId"]?{attachmentId:String(item["attachmentId"])}:{}),...(item["snapshotId"]?{snapshotId:String(item["snapshotId"])}:{}),createdAt:iso(item["createdAt"]),href:`/mdg/business-partner/requests/${encodeURIComponent(id)}#evidence-${encodeURIComponent(String(item["id"]))}`} satisfies BusinessPartner360EvidenceManifestItem)),materialization=jsonArray(row["materialization"]).map(item=>({id:String(item["id"]),childKind:String(item["childKind"]),definitionFieldCode:String(item["definitionFieldCode"]),targetType:String(item["targetType"]),targetId:String(item["targetId"]),appliedAt:iso(item["appliedAt"]),href:`/mdg/business-partner/requests/${encodeURIComponent(id)}#materialization-${encodeURIComponent(String(item["id"]))}`} satisfies BusinessPartner360MaterializationLink));return{id,requestNo:text(row,"request_no"),requestKind:text(row,"request_kind"),sourceKind:text(row,"source_kind"),...(optional(row,"requested_role")?{requestedRole:optional(row,"requested_role")} : {}),status:text(row,"status"),...(optional(row,"workflow_request_id")?{workflowRequestId:optional(row,"workflow_request_id")} : {}),createdAt:iso(row["created_at"]),createdBy:text(row,"created_by"),...(row["submitted_at"]?{submittedAt:iso(row["submitted_at"]),submittedBy:optional(row,"submitted_by")} : {}),...(row["approved_at"]?{approvedAt:iso(row["approved_at"]),approvedBy:optional(row,"approved_by")} : {}),...(row["applied_at"]?{appliedAt:iso(row["applied_at"]),appliedBy:optional(row,"applied_by")} : {}),...(optional(row,"application_result_kind")?{applicationResultKind:optional(row,"application_result_kind")} : {}),...(optional(row,"application_reason_code")?{applicationReasonCode:optional(row,"application_reason_code")} : {}),evidence,materialization,href:`/mdg/business-partner/requests/${encodeURIComponent(id)}`};}
function after(input:Input,at:string,id:string,source:string){if(!input.cursor.afterAt||!input.cursor.afterId)return sql``;if(input.cursor.afterSource&&input.cursor.afterSource!==source)return sql`AND false`;return sql`AND(${sql.raw(at)},${sql.raw(id)})<(${input.cursor.afterAt}::timestamptz,${input.cursor.afterId}::uuid)`;}function evidenceHref(row:Row,bp:string){const requestId=optional(row,"request_id");return requestId?`/mdg/business-partner/requests?targetBusinessPartnerId=${encodeURIComponent(bp)}&requestId=${encodeURIComponent(requestId)}`:undefined;}function provenance(sourceObject:string,observedAt:string){return{plane:"neon"as const,service:"master-data",sourceObject,observedAt,schemaVersion:"1"};}function text(row:Row,key:string){const value=row[key];if(value===null||value===undefined)throw new Error(`BP_360_REPOSITORY_FIELD_MISSING:${key}`);return String(value);}function optional(row:Row,key:string){const value=row[key];return value===null||value===undefined?undefined:String(value);}function iso(value:unknown){return(value instanceof Date?value:new Date(String(value))).toISOString();}function stringArray(value:unknown){return Array.isArray(value)?value.filter((item):item is string=>typeof item==="string"):[];}function jsonArray(value:unknown):Record<string,unknown>[]{return Array.isArray(value)?value.filter((item):item is Record<string,unknown>=>Boolean(item)&&typeof item==="object"&&!Array.isArray(item)):[];}
