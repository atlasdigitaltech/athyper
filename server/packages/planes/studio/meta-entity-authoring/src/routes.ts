import type { Authorizer, VerifiedRequestContext } from "@athyper/server-contract-auth";
import type { Application, RequestHandler, Response } from "express";
import type { MetaEntityGraph } from "@athyper/server-contract-meta-entity-authoring";
import { MetaEntityAuthoringService } from "./authoring-service.js";
export interface MetaEntityAuthoringRouteOptions{authenticate:RequestHandler;readContext(response:Response):VerifiedRequestContext;authorizer:Authorizer;service:MetaEntityAuthoringService;}
export function registerMetaEntityAuthoringRoutes(app:Application,o:MetaEntityAuthoringRouteOptions){
  app.post("/api/meta-entity-authoring/change-sets",o.authenticate,handler(async(q,s)=>{const c=await allowed(o,s,"metadata.entity.author");if(c)s.status(201).json(await o.service.createDraft({tenantId:c.tenantId,entityId:str(q.body,"entityId"),entityCode:str(q.body,"entityCode"),branchCode:str(q.body,"branchCode"),title:str(q.body,"title"),actorId:c.principalId}));}));
  app.put("/api/meta-entity-authoring/change-sets/:id/graph",o.authenticate,handler(async(q,s)=>{const c=await allowed(o,s,"metadata.entity.author");if(c){await scoped(o,c,uuid(q.params.id));s.json(await o.service.replaceGraph({changeSetId:uuid(q.params.id),expectedRevision:rev(q),graph:contract(q.body),actorId:c.principalId}));}}));
  action(app,o,"validate","metadata.entity.validate",(id,c)=>o.service.validate(id,c.principalId));action(app,o,"test","metadata.entity.test",(id,c)=>o.service.test(id,c.principalId));
  action(app,o,"submit","metadata.entity.submit",(id,c,q)=>o.service.submit({changeSetId:id,expectedRevision:rev(q),actorId:c.principalId}));
  action(app,o,"approve","metadata.entity.review",(id,c,q)=>o.service.approve({changeSetId:id,expectedRevision:rev(q),actorId:c.principalId,...breakGlass(q.body)}));
  action(app,o,"publish","metadata.entity.publish",(id,c,q)=>q.body?.releaseId?o.service.redispatch({releaseId:uuid(q.body.releaseId),targetPlanes:planeList(q.body)}):o.service.publish({changeSetId:id,expectedRevision:rev(q),actorId:c.principalId,targetPlanes:planeList(q.body)}),202);
  app.post("/api/meta-entity-authoring/releases/:id/activate",o.authenticate,handler(async(q,s)=>{const c=await allowed(o,s,"metadata.entity.activate",{releaseId:uuid(q.params.id)});if(c)s.json(await o.service.activate({releaseId:uuid(q.params.id),plane:onePlane(q.body),actorId:c.principalId}));}));
  app.post("/api/meta-entity-authoring/releases/:id/rollback",o.authenticate,handler(async(q,s)=>{const c=await allowed(o,s,"metadata.entity.rollback");if(c){await scoped(o,c,uuid(str(q.body,"changeSetId")));s.status(202).json(await o.service.rollback({priorReleaseId:uuid(q.params.id),changeSetId:uuid(str(q.body,"changeSetId")),expectedRevision:rev(q),actorId:c.principalId,targetPlanes:planeList(q.body)}));}}));
}
function action(app:Application,o:MetaEntityAuthoringRouteOptions,name:string,permission:string,work:(id:string,c:VerifiedRequestContext,q:any)=>Promise<unknown>,status=200){app.post(`/api/meta-entity-authoring/change-sets/:id/${name}`,o.authenticate,handler(async(q,s)=>{const c=await allowed(o,s,permission,{changeSetId:uuid(q.params.id),...(name==="publish"&&q.body?.releaseId?{releaseId:uuid(q.body.releaseId)}:{})});if(c){await scoped(o,c,uuid(q.params.id));s.status(status).json(await work(uuid(q.params.id),c,q));}}));}
async function allowed(o:MetaEntityAuthoringRouteOptions,s:Response,p:string,resource?:Readonly<Record<string,unknown>>){const c=o.readContext(s);if(c.planeKey!=="studio"||!(await o.authorizer.authorize({context:c,permissionCode:p,...(resource?{resource:{...resource,tenantId:c.tenantId}}:{})})).allowed){s.status(403).json({error:"FORBIDDEN"});return;}return c;}
function handler(fn:(q:any,s:any)=>Promise<void>):RequestHandler{return(q,s,n)=>{void fn(q,s).catch(n);};}function str(v:unknown,k:string){const x=v&&typeof v==="object"?Reflect.get(v,k):undefined;if(typeof x!=="string"||!x.trim())throw new TypeError(`${k} is required`);return x.trim();}function uuid(v:unknown){const x=String(v??"");if(!/^[0-9a-f]{8}-[0-9a-f-]{27}$/i.test(x))throw new TypeError("Invalid UUID");return x;}function rev(q:any){const x=Number(q.headers["if-match"]??q.body?.expectedRevision);if(!Number.isSafeInteger(x)||x<0)throw new TypeError("If-Match revision is required");return x;}function contract(v:unknown){if(!v||typeof v!=="object"||Reflect.get(v,"contractSchema")!=="athyper.meta-entity-contract/2.1")throw new TypeError("Contract 2.1 graph is required");return v as MetaEntityGraph;}function onePlane(v:unknown){const p=str(v,"plane");if(p!=="studio"&&p!=="neon"&&p!=="mesh")throw new TypeError("Invalid plane");return p;}function planeList(v:unknown){const p=v&&typeof v==="object"?Reflect.get(v,"targetPlanes"):undefined;if(!Array.isArray(p)||!p.length)throw new TypeError("targetPlanes are required");return p.map(x=>onePlane({plane:x}));}function breakGlass(v:unknown){const b=v&&typeof v==="object"?Reflect.get(v,"breakGlass"):undefined;if(b)throw new TypeError("Break-glass review requires server-verified approval evidence");return {};}

async function scoped(o:MetaEntityAuthoringRouteOptions,c:VerifiedRequestContext,id:string){const platform=(await o.authorizer.authorize({context:c,permissionCode:"studio.platform.catalog.manage"})).allowed;await o.service.assertTenant(id,c.tenantId,platform);}
