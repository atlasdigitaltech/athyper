import{describe,expect,it}from"vitest";
import{createStudioMetadataDraftImportAdapter}from"./metadata-draft-import.js";
const graph={contractSchema:"athyper.meta-entity-contract/2.1",entity:{entityCode:"invoice"},runtimeProfiles:[{profileKey:"default",backingKind:"virtual",apiExposure:"catalog_only",readMode:"none",writeMode:"none"}],fields:[{fieldKey:"id",dataType:"uuid",typeConfig:{kind:"uuid"}}],operations:[{operationKey:"read",operationKind:"read",label:"Read",auditEventCode:"invoice.read",fieldKeys:["id"]}]};
describe("Studio metadata draft import adapter",()=>{it("accepts a valid authored graph and rejects direct updates without a draft id",async()=>{const adapter=createStudioMetadataDraftImportAdapter(),base={context:{planeKey:"studio"},rowNumber:1,scope:{constraints:[]}};await expect(adapter.validate({...base,session:{operation:"create"},row:{entity_id:"018f6d2a-aaaa-7aaa-8aaa-aaaaaaaaaaaa",entity_code:"invoice",branch_code:"import",title:"Imported invoice",graph}}as never)).resolves.toEqual({rowNumber:1,valid:true,errors:[]});const result=await adapter.validate({...base,session:{operation:"update"},row:{graph}}as never);expect(result.valid).toBe(false);expect(result.errors.join(" ")).toContain("IMPORT_CHANGE_SET_REQUIRED");});});
