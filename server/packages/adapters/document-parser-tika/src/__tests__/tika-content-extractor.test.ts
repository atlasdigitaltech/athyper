import {describe,expect,it,vi} from "vitest";
import {createTikaContentExtractor} from "../index.js";
describe("Tika content extractor",()=>{it("sends bounded bytes and normalizes extracted text",async()=>{const fetch=vi.fn(async()=>new Response(" hello\u0000 world ",{status:200,headers:{"content-type":"text/plain"}}));const extractor=createTikaContentExtractor({baseUrl:"http://docparser:9998",fetch:fetch as never});await expect(extractor.extract({content:new Uint8Array([1,2]),contentType:"application/pdf",fileName:"a.pdf"})).resolves.toMatchObject({text:"hello world",provider:"apache-tika"});expect(fetch).toHaveBeenCalledWith("http://docparser:9998/tika",expect.objectContaining({method:"PUT"}));});it("enforces input size before HTTP",async()=>{const extractor=createTikaContentExtractor({baseUrl:"http://docparser:9998",maxInputBytes:1,fetch:vi.fn() as never});await expect(extractor.extract({content:new Uint8Array(2),contentType:"application/pdf"})).rejects.toMatchObject({code:"EXTRACTION_SIZE_LIMIT"});});});
