import type {Authorizer} from "@athyper/server-contract-auth";import type {DocumentSearchService,SearchDocumentsCommand,SearchIndex} from "@athyper/server-contract-search";
export class SearchError extends Error{constructor(readonly statusCode:number,readonly code:string,message:string){super(message);this.name="SearchError";}}
export function createDocumentSearchService(options:{readonly authorizer:Authorizer;readonly index:SearchIndex}):DocumentSearchService{return {async search(command:SearchDocumentsCommand){const authorization=await options.authorizer.authorize({context:command.context,permissionCode:"documents.search"});if(!authorization.allowed)throw new SearchError(403,"FORBIDDEN","Document search is not permitted");const text=command.text.trim();if(!text||text.length>256)throw new SearchError(400,"INVALID_SEARCH_QUERY","Search text must contain 1-256 characters");const entityTypes=command.entityTypes?.map((value)=>value.trim()).filter(Boolean);if(entityTypes&&entityTypes.some((value)=>!/^[a-z][a-z0-9_.-]{1,126}$/.test(value)))throw new SearchError(400,"INVALID_ENTITY_TYPE","Invalid entity type filter");const page=integer(command.page??1,1,10_000,"page");const pageSize=integer(command.pageSize??20,1,100,"pageSize");const result=await options.index.search({planeKey:command.context.planeKey,tenantId:command.context.tenantId,text,...(entityTypes?.length?{entityTypes:[...new Set(entityTypes)]}:{}),limit:Math.min(500,pageSize*5),offset:(page-1)*pageSize});const visible=[];for(const hit of result.hits){const decision=await options.authorizer.authorize({context:command.context,permissionCode:"documents.read",resource:{attachmentId:hit.attachmentId,entityType:hit.entityType,entityId:hit.entityId}});if(decision.allowed)visible.push(hit);if(visible.length===pageSize)break;}return {...result,hits:visible,total:visible.length,page,pageSize};}};}
function integer(value:number,min:number,max:number,name:string){if(!Number.isInteger(value)||value<min||value>max)throw new SearchError(400,"INVALID_PAGINATION",`${name} must be between ${min} and ${max}`);return value;}
