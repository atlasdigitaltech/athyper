import type {VerifiedRequestContext} from "@athyper/server-contract-auth";import type {DocumentSearchService} from "@athyper/server-contract-search";import type {Application,RequestHandler,Response} from "express";import {SearchError} from "./document-search-service.js";
export function registerDocumentSearchRoutes(application:Application,options:{readonly authenticate:RequestHandler;readonly readContext:(response:Response)=>VerifiedRequestContext;readonly search:DocumentSearchService}){application.get("/api/search/documents",options.authenticate,async(request,response,next)=>{try{const text=typeof request.query["q"]==="string"?request.query["q"]:"";const rawTypes=typeof request.query["entity_type"]==="string"?request.query["entity_type"].split(","):undefined;const result=await options.search.search({context:options.readContext(response),text,...(rawTypes?{entityTypes:rawTypes}:{}),page:number(request.query["page"]),pageSize:number(request.query["page_size"])});response.status(200).json(result);}catch(error){if(error instanceof SearchError)response.status(error.statusCode).json({error:error.code,message:error.message});else next(error);}});}
function number(value:unknown){if(typeof value!=="string"||!value)return undefined;const parsed=Number(value);return Number.isInteger(parsed)?parsed:Number.NaN;}
