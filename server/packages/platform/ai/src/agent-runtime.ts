import { createHash, randomUUID } from "node:crypto";
import type { AtlasContentBlock, AtlasDataClass, AtlasFinishReason, AtlasModelBinding, AtlasModelPolicyResolver, AtlasPlaneAdmissionResolver, AtlasProviderCredentialLease, AtlasProviderCredentialResolver, AtlasProviderError, AtlasProviderUsage, AtlasPublicModelId, AtlasQuotaReservation, AtlasRunRepository, AtlasSseEnvelope, AtlasTenantQuotaManager, AtlasUsageLedger } from "@athyper/server-contract-ai";
import type { VerifiedRequestContext } from "@athyper/server-contract-auth";
import { AtlasBindingRegistry, AtlasProviderRegistry } from "./bindings.js";
import { assertAtlasContext } from "./context.js";
import { AtlasServiceError } from "./errors.js";
import type { AtlasRuntimeToolCoordinator } from "./runtime-tool-coordinator.js";
import { AtlasThreadService } from "./thread-service.js";

export interface AtlasPromptResolver { resolve(input: { readonly context: VerifiedRequestContext; readonly promptRevision: string }): Promise<{ readonly revision: string; readonly systemText: string }> }
export interface AtlasAgentRuntimeOptions {
  readonly admission: AtlasPlaneAdmissionResolver; readonly modelPolicy: AtlasModelPolicyResolver; readonly bindings: AtlasBindingRegistry; readonly providers: AtlasProviderRegistry; readonly credentials: AtlasProviderCredentialResolver;
  readonly threads: AtlasThreadService; readonly runs: AtlasRunRepository; readonly ledger: AtlasUsageLedger; readonly prompts: AtlasPromptResolver; readonly quota?: AtlasTenantQuotaManager; readonly tools?: AtlasRuntimeToolCoordinator;
  readonly maxInputCharacters: number; readonly maxToolRounds: number; readonly now?: () => Date; readonly createId?: () => string;
}
export interface AtlasRunCommand { readonly context: VerifiedRequestContext; readonly threadId: string; readonly clientRequestId: string; readonly publicModelId: AtlasPublicModelId; readonly dataClass: AtlasDataClass; readonly userText: string; readonly catalogPolicyRevision: string; readonly signal?: AbortSignal }

export class AtlasAgentRuntime {
  private readonly now: () => Date; private readonly createId: () => string;
  constructor(private readonly options: AtlasAgentRuntimeOptions) { this.now = options.now ?? (() => new Date()); this.createId = options.createId ?? randomUUID; if (options.maxInputCharacters < 1 || options.maxToolRounds < 0) throw new TypeError("Atlas runtime limits are invalid."); }
  async *run(command: AtlasRunCommand): AsyncIterable<AtlasSseEnvelope> {
    assertAtlasContext(command.context); const userText = command.userText.trim(); if (!userText || userText.length > this.options.maxInputCharacters) throw new AtlasServiceError("INVALID_ARGUMENT", "Atlas user input is outside the configured bounds.");
    const admission = await this.options.admission.resolve(command.context); if (!admission.chatAllowed || !admission.persistenceAllowed) throw new AtlasServiceError("ADMISSION_DENIED", "Durable Atlas chat is not admitted for this request.");
    if (!admission.allowedPublicModelIds.includes(command.publicModelId) || !admission.allowedDataClasses.includes(command.dataClass)) throw new AtlasServiceError("ADMISSION_DENIED", "The requested Atlas mode or data class is not admitted.");
    if (command.catalogPolicyRevision !== admission.policyRevision) throw new AtlasServiceError("BINDING_POLICY_DENIED", "The Atlas catalog revision is stale.");
    const binding = this.options.bindings.resolve(command.publicModelId); const policy = await this.options.modelPolicy.evaluate({ context: command.context, admission, binding, dataClass: command.dataClass });
    if (!policy.allowed) throw new AtlasServiceError("BINDING_POLICY_DENIED", "The exact Atlas model binding was denied by policy.");
    const prompt = await this.options.prompts.resolve({ context: command.context, promptRevision: policy.promptRevision }); if (prompt.revision !== policy.promptRevision) throw new AtlasServiceError("BINDING_POLICY_DENIED", "The Atlas prompt revision changed before execution.");
    const thread = await this.options.threads.get(command.context, command.threadId); if (thread.status !== "active") throw new AtlasServiceError("THREAD_NOT_ACTIVE", "Atlas runs require an active thread.");
    const history = await this.options.threads.boundedHistory(command.context, command.threadId); const runId = this.createId(); const outputMessageId = this.createId(); const begin = await this.options.runs.begin({ context: command.context, runId, threadId: thread.threadId, clientRequestId: required(command.clientRequestId), inputMessageId: this.createId(), outputMessageId, userContent: [{ type: "text", text: userText }], publicModelId: binding.publicModelId, bindingId: binding.bindingId, bindingRevision: binding.bindingRevision, policyRevision: policy.policyRevision, promptRevision: policy.promptRevision, startedAt: this.now().toISOString() });
    let sequence = 0; const envelope = (event: AtlasSseEnvelope["event"]): AtlasSseEnvelope => ({ protocol: "atlas.sse/1", sequence: ++sequence, runId: begin.run.runId, threadId: thread.threadId, emittedAt: this.now().toISOString(), event });
    if (begin.replayed) { yield envelope({ type: "run.started", publicModelId: binding.publicModelId, bindingRevision: binding.bindingRevision, policyRevision: policy.policyRevision, promptRevision: policy.promptRevision }); for (const block of begin.replayedOutput ?? []) if (block.type === "text") yield envelope({ type: "message.delta", messageId: begin.run.outputMessageId, text: block.text }); if (begin.run.status === "completed") yield envelope({ type: "run.completed", messageId: begin.run.outputMessageId, reason: "stop" }); else if (begin.run.status === "cancelled") yield envelope({ type: "run.cancelled" }); else if (begin.run.status === "failed") yield envelope({ type: "run.failed", errorClass: begin.run.terminalErrorClass ?? "upstream_error", code: "replayed_failure", retryable: false }); return; }
    let reservation:AtlasQuotaReservation|undefined;let chargedUsage:AtlasProviderUsage={};
    try {
      if(this.options.quota){try{reservation=await this.options.quota.reserve({context:command.context,estimatedInputTokens:estimateTokens(userText),maxOutputTokens:binding.capabilities.maxOutputTokens});}catch(error){await this.options.runs.fail({context:command.context,runId:begin.run.runId,errorClass:"quota_exhausted",failedAt:this.now().toISOString()});throw error;}}
      if(command.signal?.aborted){await this.options.runs.cancel({context:command.context,runId:begin.run.runId,cancelledAt:this.now().toISOString()});yield envelope({type:"run.cancelled"});return;}
      yield envelope({ type: "run.started", publicModelId: binding.publicModelId, bindingRevision: binding.bindingRevision, policyRevision: policy.policyRevision, promptRevision: policy.promptRevision });
      const principalHash = sha(`${command.context.tenantId}\u0000${command.context.principalId}`); const messages = [...history, { role: "user" as const, content: [{ type: "text" as const, text: userText }] }]; const persisted: AtlasContentBlock[] = [];
      const definitions = admission.readToolsAllowed && binding.capabilities.tools && this.options.tools ? await this.options.tools.definitions(command.context) : [];
      const candidates=this.options.bindings.resolveChain(binding);for(const candidate of candidates.slice(1)){if(!candidate.allowedDataClasses.includes(command.dataClass))throw new AtlasServiceError("BINDING_POLICY_DENIED","An Atlas fallback binding does not admit the requested data class.");const fallbackPolicy=await this.options.modelPolicy.evaluate({context:command.context,admission,binding:candidate,dataClass:command.dataClass});if(!fallbackPolicy.allowed||fallbackPolicy.policyRevision!==policy.policyRevision||fallbackPolicy.promptRevision!==policy.promptRevision)throw new AtlasServiceError("BINDING_POLICY_DENIED","An Atlas fallback binding was denied by the pinned policy.");}
      for (let round = 0; round <= this.options.maxToolRounds; round += 1) {
        let terminalFailure:AtlasProviderError|null=null;let terminalCancelled=false;let finish:AtlasFinishReason="error";let acceptedBlocks:AtlasContentBlock[]=[];let acceptedToolCalls:{callId:string;toolName:string;input:Readonly<Record<string,unknown>>}[]=[];
        for(let attempt=0;attempt<candidates.length;attempt+=1){
          const candidate=candidates[attempt]!;let credential:AtlasProviderCredentialLease;try{credential=await this.credential(command.context.tenantId,candidate);}catch{const unavailable:AtlasProviderError={errorClass:"authentication",code:"credential_unavailable",safeMessage:"The Atlas provider credential is unavailable.",retryable:true};if(attempt+1<candidates.length)continue;terminalFailure=unavailable;break;}const provider=this.options.providers.resolve(candidate);const providerCallId=this.createId();const callStarted=this.now();let providerRequestId:string|null=null;let actualModelId="";let usage:AtlasProviderUsage={};let failure:AtlasProviderError|null=null;let cancelled=false;let exposed=false;const roundBlocks:AtlasContentBlock[]=[];const toolCalls:{callId:string;toolName:string;input:Readonly<Record<string,unknown>>}[]=[];
          try{for await(const event of provider.invoke({binding:candidate,credential,prompt:{messages:[{role:"system",content:[{type:"text",text:prompt.systemText}]},...messages],maxOutputTokens:candidate.capabilities.maxOutputTokens,...(definitions.length?{tools:definitions}:{})},signal:command.signal,trace:{runId:begin.run.runId,providerCallId,tenantId:command.context.tenantId,principalHash,safetyIdentifier:principalHash,promptRevision:policy.promptRevision,policyRevision:policy.policyRevision}})){
            if(command.signal?.aborted){cancelled=true;finish="cancelled";break;}
            if(event.kind==="response_started"){providerRequestId=event.providerRequestId;actualModelId=event.actualModelId;if(actualModelId!==candidate.upstreamModelId){failure={errorClass:"protocol_error",code:"actual_model_mismatch",safeMessage:"The Atlas provider returned an unexpected model.",retryable:false};break;}}
            else if(event.kind==="text_delta"){exposed=true;roundBlocks.push({type:"text",text:event.text});yield envelope({type:"message.delta",messageId:begin.run.outputMessageId,text:event.text});}
            else if(event.kind==="tool_call_complete"){exposed=true;toolCalls.push({callId:event.callId,toolName:event.toolName,input:event.input});roundBlocks.push({type:"tool_use",callId:event.callId,toolName:event.toolName,input:event.input});}
            else if(event.kind==="usage"){usage=mergeUsage(usage,event.usage,event.mode);yield envelope({type:"usage.updated",usage:mergeUsage(chargedUsage,usage,"delta")});}
            else if(event.kind==="completed")finish=event.reason;else if(event.kind==="failed"){failure=event.error;finish="error";}else if(event.kind==="cancelled"){cancelled=true;finish="cancelled";}
          }}catch{if(command.signal?.aborted){cancelled=true;finish="cancelled";}else{failure={errorClass:"upstream_error",code:"provider_adapter_failed",safeMessage:"The Atlas provider request could not be completed.",retryable:true};finish="error";}}
          chargedUsage=mergeUsage(chargedUsage,usage,"delta");await this.options.ledger.append(contentFreeLedger({ledgerId:this.createId(),runId:begin.run.runId,providerCallId,context:command.context,principalHash,binding:candidate,credential,providerRequestId,actualModelId:actualModelId||candidate.upstreamModelId,policyRevision:policy.policyRevision,promptRevision:policy.promptRevision,usage,finish,failure,durationMs:Math.max(0,this.now().getTime()-callStarted.getTime()),recordedAt:this.now().toISOString()}));
          if(cancelled){terminalCancelled=true;break;}if(failure&&failure.retryable&&!exposed&&attempt+1<candidates.length)continue;terminalFailure=failure;acceptedBlocks=roundBlocks;acceptedToolCalls=toolCalls;break;
        }
        persisted.push(...acceptedBlocks);
        if(terminalCancelled){await this.options.runs.cancel({context:command.context,runId:begin.run.runId,cancelledAt:this.now().toISOString()});yield envelope({type:"run.cancelled"});return;}
        if(terminalFailure){await this.options.runs.fail({context:command.context,runId:begin.run.runId,errorClass:terminalFailure.errorClass,failedAt:this.now().toISOString()});yield envelope({type:"run.failed",errorClass:terminalFailure.errorClass,code:terminalFailure.code,retryable:terminalFailure.retryable});return;}
        if(acceptedToolCalls.length){if(!this.options.tools||!admission.readToolsAllowed){await this.options.runs.fail({context:command.context,runId:begin.run.runId,errorClass:"protocol_error",failedAt:this.now().toISOString()});yield envelope({type:"run.failed",errorClass:"protocol_error",code:"tool_not_admitted",retryable:false});return;}const results:AtlasContentBlock[]=[];let awaitingConfirmation=false;for(const call of acceptedToolCalls){const outcome=await this.options.tools.handle({context:command.context,runId:begin.run.runId,threadId:thread.threadId,callId:call.callId,toolCode:call.toolName,arguments:call.input,signal:command.signal});yield envelope({type:"tool.previewed",callId:call.callId,toolCode:call.toolName,proposalId:outcome.preview.proposalId,confirmationRequired:outcome.preview.confirmationRequired});if(outcome.result){yield envelope({type:"tool.completed",callId:call.callId,toolCode:call.toolName,outcome:outcome.result.outcome});results.push({type:"tool_result",callId:call.callId,toolName:call.toolName,result:outcome.result.data??{outcome:outcome.result.outcome},isError:outcome.result.outcome!=="completed"});}else awaitingConfirmation=true;}if(awaitingConfirmation){await this.options.runs.complete({context:command.context,runId:begin.run.runId,assistantContent:persisted,completedAt:this.now().toISOString()});yield envelope({type:"run.completed",messageId:begin.run.outputMessageId,reason:"tool_call"});return;}messages.push({role:"assistant",content:acceptedBlocks},{role:"tool",content:results});persisted.push(...results);continue;}
        await this.options.runs.complete({context:command.context,runId:begin.run.runId,assistantContent:persisted,completedAt:this.now().toISOString()});yield envelope({type:"run.completed",messageId:begin.run.outputMessageId,reason:finish==="error"?"stop":finish});return;
      }
      await this.options.runs.fail({context:command.context,runId:begin.run.runId,errorClass:"protocol_error",failedAt:this.now().toISOString()});yield envelope({type:"run.failed",errorClass:"protocol_error",code:"tool_round_limit",retryable:false});
    } catch(error){if(command.signal?.aborted){await this.options.runs.cancel({context:command.context,runId:begin.run.runId,cancelledAt:this.now().toISOString()});yield envelope({type:"run.cancelled"});return;}if(!(error instanceof AtlasServiceError&&error.code==="QUOTA_EXCEEDED"))await this.options.runs.fail({context:command.context,runId:begin.run.runId,errorClass:error instanceof AtlasServiceError&&error.code==="CREDENTIAL_UNAVAILABLE"?"authentication":"upstream_error",failedAt:this.now().toISOString()});throw error;} finally {if(this.options.quota&&reservation)await this.options.quota.settle({reservation,inputTokens:totalInput(chargedUsage),outputTokens:totalOutput(chargedUsage)});}
  }
  private async credential(tenantId:string,binding:AtlasModelBinding):Promise<AtlasProviderCredentialLease>{const value=await this.options.credentials.resolve({tenantId,binding});if(!value||value.ownerId!==binding.credentialOwnerId)throw new AtlasServiceError("CREDENTIAL_UNAVAILABLE","The exact Atlas provider credential is unavailable.");return value;}
}

function contentFreeLedger(input:any){const b=input.binding,u=input.usage as AtlasProviderUsage,inputCost=cost(totalInput(u),b.inputPricePerMtokUsd),outputCost=cost(totalOutput(u),b.outputPricePerMtokUsd);return{ledgerId:input.ledgerId,runId:input.runId,providerCallId:input.providerCallId,tenantId:input.context.tenantId,planeKey:input.context.planeKey,principalHash:input.principalHash,providerId:b.providerId,providerRequestId:input.providerRequestId,credentialId:input.credential.credentialId,credentialRevision:input.credential.credentialRevision,credentialOwnerId:input.credential.ownerId,providerRegion:b.providerRegion,publicModelId:b.publicModelId,bindingId:b.bindingId,bindingRevision:b.bindingRevision,actualModelId:input.actualModelId,adapterId:b.adapterId,adapterVersion:b.adapterVersion,policyRevision:input.policyRevision,promptRevision:input.promptRevision,priceVersion:b.priceVersion,usage:u,inputCostUsd:inputCost,outputCostUsd:outputCost,totalCostUsd:inputCost===null||outputCost===null?null:inputCost+outputCost,finishReason:input.finish,errorClass:input.failure?.errorClass??null,durationMs:input.durationMs,recordedAt:input.recordedAt};}
function mergeUsage(current:AtlasProviderUsage,next:AtlasProviderUsage,mode:"snapshot"|"delta"):AtlasProviderUsage{const merge=(a:number|undefined,b:number|undefined)=>mode==="delta"?(a??0)+(b??0):b??a;return{inputTokens:merge(current.inputTokens,next.inputTokens),outputTokens:merge(current.outputTokens,next.outputTokens),cacheReadTokens:merge(current.cacheReadTokens,next.cacheReadTokens),cacheWriteTokens:merge(current.cacheWriteTokens,next.cacheWriteTokens),reasoningTokens:merge(current.reasoningTokens,next.reasoningTokens)};}
function totalInput(u:AtlasProviderUsage):number{return(u.inputTokens??0)+(u.cacheReadTokens??0)+(u.cacheWriteTokens??0);}function totalOutput(u:AtlasProviderUsage):number{return(u.outputTokens??0)+(u.reasoningTokens??0);}function estimateTokens(text:string):number{return Math.max(1,Math.ceil(text.length/4));}
function cost(tokens:number,price:number|null):number|null{return price===null?null:tokens*price/1_000_000;}function sha(value:string):string{return createHash("sha256").update(value).digest("hex");}function required(value:string):string{const result=value.trim();if(!result)throw new AtlasServiceError("INVALID_ARGUMENT","Atlas client request id is required.");return result;}
