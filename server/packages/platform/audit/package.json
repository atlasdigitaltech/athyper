{
  "name": "@athyper/server-platform-audit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@athyper/server-contract-audit": "workspace:*",
    "@athyper/server-contract-auth": "workspace:*",
    "express": "^5.0.0",
    "@athyper/server-runtime-http": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/express": "^5.0.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.4"
  }
}
