import{describe,expect,it}from"vitest";import{assertLegacyAuthorizationCompatible,runLegacyAuthorizationCompatibilitySuite}from"../legacy-compatibility.js";
describe("IAM legacy compatibility",()=>{it("classifies security regressions and documented cutover differences",async()=>{const report=await runLegacyAuthorizationCompatibilitySuite({cases:[{id:"same",input:"same"},{id:"broadened",input:"broadened"},{id:"legacy-claim-cutover",input:"cutover",expectedDifference:"legacy_only"}],legacy:{decide:async value=>({allowed:value==="cutover"})},canonical:{decide:async value=>({allowed:value==="broadened"})}});expect(report).toMatchObject({compatible:false,total:3,equivalent:1,expectedDifferences:1,securityRegressions:1});expect(()=>assertLegacyAuthorizationCompatible(report)).toThrow("broadened:security_regression");});});
