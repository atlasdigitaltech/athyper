import{describe,expect,it,vi}from"vitest";import{createWorkflowSlaAutomation,scheduleSla,type SlaBreachCandidate}from"../index.js";
describe("workflow SLA automation",()=>{it("normalizes schedules and escalates through an active delegation",async()=>{expect(scheduleSla({code:"approval",version:2,durationMinutes:60,reminderMinutes:[30,10,30],escalationMinutes:0},new Date("2026-08-12T00:00:00Z"))).toEqual({policyCode:"approval",policyVersion:2,dueAt:"2026-08-12T01:00:00.000Z",remindersAt:["2026-08-12T00:10:00.000Z","2026-08-12T00:30:00.000Z"],escalateAt:"2026-08-12T00:00:00.000Z"});const candidate:SlaBreachCandidate={id:"item",tenantId:"tenant",assigneePrincipalId:"owner",dueAt:"2026-08-11T00:00:00Z",rowVersion:1,payload:{}};const record=vi.fn().mockResolvedValue(true),automation=createWorkflowSlaAutomation({repository:{claim:vi.fn().mockResolvedValue([candidate]),record},transactions:{run:async(_plane,_actor,work)=>work({})},delegations:{resolve:vi.fn().mockResolvedValue("delegate")},now:()=>new Date("2026-08-12T00:00:00Z")});await expect(automation.sweep({planeKey:"neon",tenantId:"tenant",principalId:"system"})).resolves.toEqual({inspected:1,breached:0,escalated:1,skipped:0});expect(record).toHaveBeenCalledWith(expect.objectContaining({candidate,escalatedTo:"delegate"}),{});});});
