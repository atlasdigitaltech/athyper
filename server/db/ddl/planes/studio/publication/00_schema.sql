CREATE SCHEMA publication;

COMMENT ON SCHEMA publication IS
  'Athyper-only append-only release, artifact, deployment, and acknowledgement authority for signed definitions published to Athyper, Neon, and Mesh runtimes.';
