CREATE SCHEMA onboarding;

COMMENT ON SCHEMA onboarding IS
  'Athyper-only guest-safe organization onboarding, expansion, subscription-change, and offboarding orchestration state. Journey definitions remain centralized in metadata.entity_flow.';
