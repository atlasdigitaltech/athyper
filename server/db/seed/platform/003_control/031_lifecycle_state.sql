-- Table-owned seed for control.lifecycle_state
-- Rows are emitted by compound lifecycle blocks in 030_lifecycle.sql so lifecycle_id resolution stays local to each lifecycle definition.
-- Lookup domain/value seeds remain under 000_lookups by design.

