-- Table-owned seed for control.workflow_template_stage
-- Rows are emitted by compound workflow-template blocks in 060_workflow_template.sql so workflow_template_id resolution stays local to each workflow definition.
-- Lookup domain/value seeds remain under 000_lookups by design.

