-- Table-owned seed for control.lifecycle_transition
-- Rows are emitted by compound lifecycle blocks in 030_lifecycle.sql so from/to lifecycle_state ids stay local to each lifecycle definition.
-- Lookup domain/value seeds remain under 000_lookups by design.

