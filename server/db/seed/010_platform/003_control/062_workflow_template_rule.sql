-- Table-owned seed for control.workflow_template_rule
-- Rows are emitted by compound workflow-template blocks in 060_workflow_template.sql so workflow_template_id/stage_no resolution stays local to each workflow definition.
-- Lookup domain/value seeds remain under 000_lookups by design.

