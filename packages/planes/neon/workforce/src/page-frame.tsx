"use client";
import { PlanePageFrame } from "@athyper/platform-shell";

/** Thin alias for the platform's PlanePageFrame — promoted there once this plane needed the identical pattern already used by Business Partner. */
export const WorkforcePageFrame = PlanePageFrame;
