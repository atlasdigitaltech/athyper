"use client";
import { PlanePageFrame } from "@athyper/platform-shell";

/** Shared page frame for BP surfaces. Thin alias for the platform's PlanePageFrame — promoted there once the Workforce plane needed the identical pattern. */
export const BusinessPartnerPageFrame = PlanePageFrame;
