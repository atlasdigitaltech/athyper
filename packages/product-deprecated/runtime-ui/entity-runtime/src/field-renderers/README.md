# Field Renderers

This folder is reserved for field renderer registry and default renderer wiring
for `@athyper/entity-runtime/field-renderers`.

Feature-level field value components should live in a `fields/` folder in the
owning runtime package. Descriptor-rendered page sections belong in `surfaces/`;
platform drawers and side panels belong in `panels/`.
