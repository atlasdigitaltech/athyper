# Meta Entity runtime

Framework-independent authoring state and selectors for the canonical Athyper
`metadata.*` model.

Forbidden dependencies include the legacy metadata service, legacy Studio UI,
`control.entity_*`, and versioned legacy Meta Entity contracts.
