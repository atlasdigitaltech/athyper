// Re-exported from @athyper/ui/layout so callers within entity-runtime can use
// the short relative path while document-runtime imports from the ui package.
export { PageHeader, TypeChip, ModeBadge, type PageHeaderProps } from "@athyper/ui/layout";
