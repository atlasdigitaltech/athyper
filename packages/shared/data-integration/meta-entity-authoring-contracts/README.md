# Meta Entity authoring contracts

Clean transport and editor contracts for the Athyper `metadata.*` model.

This package must not import legacy Meta Entity contracts or expose
`control.entity_*` compatibility shapes.
